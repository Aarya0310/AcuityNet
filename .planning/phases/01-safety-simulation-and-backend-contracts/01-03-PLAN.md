---
phase: 01-safety-simulation-and-backend-contracts
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [backend/app/contracts/patients.py, backend/app/contracts/vitals.py, backend/app/contracts/configuration.py, backend/app/transport/vitals.py, backend/app/transport/configuration.py, backend/tests/test_vital_contracts.py, backend/tests/test_vitals_api.py]
autonomous: true
requirements: []
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths: ["Current reads expose all required typed fields and resolved freshness states.", "A typed endpoint publishes 5/10/30/manual intervals and backend advancement precedes authoritative GET."]
  artifacts: ["backend/app/contracts/vitals.py", "backend/app/contracts/configuration.py", "backend/app/transport/configuration.py"]
  key_links: ["service -> typed current-vitals REST response", "configuration GET -> supported interval contract", "advance -> authoritative current GET"]
---
<objective>Expose typed patient/vital contracts, resolved freshness, and server-owned refresh configuration.</objective>
<execution_context>@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-RESEARCH.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-02-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add typed current-vitals and freshness contracts</name>
<files>backend/app/contracts/patients.py, backend/app/contracts/vitals.py, backend/app/transport/vitals.py, backend/tests/test_vital_contracts.py, backend/tests/test_vitals_api.py</files>
<behavior><p>Test 1: Current response contains bed, six units, sequence, timestamps, synthetic provenance, false bedside flag, and label.</p><p>Test 2: Freshness is fresh at <=15 seconds, stale at >15 and <=60, disconnected on transport failure, and unavailable without observation.</p><p>Test 3: Retrospective/replay provenance and unbounded advancement are rejected.</p></behavior>
<action>Map service output to Pydantic DTOs and expose public read-only current-vitals plus a bounded development fixture advance. Apply server-owned provenance and the resolved 15/60-second policy; preserve the Phase 2 JWT seam.</action>
<verify><automated>python -m pytest backend/tests/test_vital_contracts.py backend/tests/test_vitals_api.py -q</automated></verify>
<done>Monitoring fields, freshness, provenance, and bounded advancement are validated at the API boundary.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Publish interval configuration and authoritative automatic operation</name>
<files>backend/app/contracts/configuration.py, backend/app/transport/configuration.py, backend/tests/test_vitals_api.py</files>
<behavior><p>Test 1: Configuration GET returns exactly 5, 10, 30, manual with default 10.</p><p>Test 2: Automatic operation advances one backend logical tick and equals a subsequent current GET.</p></behavior>
<action>Create the typed configuration read endpoint and wire bounded automatic operation to advance on the backend before returning or refetching authoritative REST state. Reject unsupported intervals and unbounded counts; browser refetch alone must not advance the scenario.</action>
<verify><automated>python -m pytest backend/tests/test_vitals_api.py -q</automated></verify>
<done>Supported intervals are server-defined and automatic progression is backend-owned.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Client to monitoring API | Patient, interval, and advancement inputs are untrusted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Tampering | API contracts | high | mitigate | Validate ranges/enums, bound advancement, and construct provenance server-side. |
| T-01-SC | Tampering | Package install | high | mitigate | Registry checks in Plan 01 precede SUS installation. |
</threat_model>
<verification><automated>python -m pytest backend/tests/test_vital_contracts.py backend/tests/test_vitals_api.py -q</automated></verification>
<success_criteria>Backend contracts express the resolved vital, freshness, interval, REST-authority, and provenance decisions.</success_criteria>
<output>Create `.planning/phases/01-safety-simulation-and-backend-contracts/01-03-SUMMARY.md` when done.</output>
