---
phase: 01-safety-simulation-and-backend-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, backend/pyproject.toml, backend/alembic.ini, backend/app/main.py, backend/app/contracts/vitals.py, backend/app/persistence/database.py, backend/app/persistence/models.py, backend/app/migrations/env.py, backend/app/migrations/versions/0001_phase1_foundation.py, backend/app/seed/demo_data.py, backend/tests/test_walking_skeleton.py]
autonomous: true
requirements: [DATA-01, DATA-02]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths: ["A migrated SQLite database contains minimal P-1042 before observation write.", "A backend test proves one bounded synthetic observation write/read with safety metadata."]
  artifacts: ["backend/app/migrations/versions/0001_phase1_foundation.py", "backend/app/seed/demo_data.py", "backend/tests/test_walking_skeleton.py"]
  key_links: ["fixture -> migration -> seed -> observation API -> current REST response"]
---
<objective>Build the smallest backend walking skeleton and executable test, including migration and minimal P-1042 prerequisite. This plan retains the backend bootstrap, migration, and tracer fixture files because the first vertical path must execute from an empty database.</objective>
<execution_context>@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-RESEARCH.md
@.planning/phases/01-safety-simulation-and-backend-contracts/SKELETON.md</context>
<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Create backend tracer test and migration-backed path</name>
<files>pyproject.toml, backend/pyproject.toml, backend/alembic.ini, backend/app/main.py, backend/app/contracts/vitals.py, backend/app/persistence/database.py, backend/app/persistence/models.py, backend/app/migrations/env.py, backend/app/migrations/versions/0001_phase1_foundation.py, backend/tests/test_walking_skeleton.py</files>
<behavior><p>Test 1: Temporary empty SQLite migrates and the tracer fixture explicitly creates the minimal P-1042 patient, admission, bed, nurse, history, and configuration rows before the observation write.</p><p>Test 2: One bounded synthetic observation returns six vitals, timestamps, freshness, provenance, and label.</p></behavior>
<action>Before installing the selected packages, run concrete registry checks and record each result: `python -m pip index versions fastapi`, `python -m pip index versions sqlalchemy`, `python -m pip index versions alembic`, `python -m pip index versions pydantic`, `python -m pip index versions uvicorn`, `python -m pip index versions pytest`, `python -m pip index versions httpx`, `npm view react version`, `npm view vite version`, `npm view vitest version`, and `npm view @testing-library/react version`. Stop when identity or repository is unverified and record the command output in the implementation summary before any SUS installation. Create `backend/alembic.ini` as the checked-in Alembic configuration used by the migration environment and subsequent `alembic --config alembic.ini ...` commands. Create the test before implementation. Put the minimal fictional P-1042 aggregate directly in the tracer fixture so its write/read path has a seeded prerequisite; Plan 02 then extracts and expands that fixture into the reusable idempotent seed/reset command. Use SQLAlchemy 2, Alembic, UTC timestamps, server-owned provenance, and bounded fixture advancement. Per the resolved Phase 1 access decision, current-vitals reads are public read-only synthetic monitoring while JWT mutations remain deferred.</action>
<verify><automated>cd backend; alembic --config alembic.ini upgrade head; alembic --config alembic.ini check; cd ..; python -m pytest backend/tests/test_walking_skeleton.py -q</automated></verify>
<done>The pre-existing test proves the single migration-backed backend path end to end.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Seed the minimal P-1042 prerequisite</name>
<files>backend/app/seed/demo_data.py, backend/tests/test_walking_skeleton.py</files>
<behavior><p>Test 1: Fixture creates one P-1042 patient, admission, ICU bed, nurse, history, and configuration before observation write.</p><p>Test 2: Repeated fixture setup does not duplicate stable IDs.</p></behavior>
<action>Implement the smallest idempotent fictional P-1042 aggregate, separate from migration execution, with the resolved 5/10/30/manual configuration shape and no retrospective rows.</action>
<verify><automated>python -m pytest backend/tests/test_walking_skeleton.py -q</automated></verify>
<done>The tracer has its explicit seeded prerequisite and repeatable fixture.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser to FastAPI | Patient IDs and trigger parameters are untrusted. |
| FastAPI to SQLite | Validated synthetic records become durable. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | DTO and trigger | high | mitigate | Bound inputs, validate ranges, and construct provenance on server. |
| T-01-SC | Tampering | Package install | high | mitigate | Run registry identity checks before SUS installation. |
</threat_model>
<verification><automated>python -m pytest backend/tests/test_walking_skeleton.py -q</automated></verification>
<success_criteria>Migration, minimal seed, and tested API write/read path exist without frontend/documentation ownership.</success_criteria>
<output>Create `.planning/phases/01-safety-simulation-and-backend-contracts/01-01-SUMMARY.md` when done.</output>
