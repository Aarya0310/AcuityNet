---
phase: 01-safety-simulation-and-backend-contracts
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [backend/app/seed/reset.py, backend/app/seed/demo_data.py, backend/app/vitals/scenario.py, backend/app/vitals/service.py, backend/tests/test_seed.py, backend/tests/test_scenario.py]
autonomous: true
requirements: [VITAL-01, VITAL-02]
estimate: {tokens: 15000, raw_tokens: 15000, tasks: 2, confidence: low}
must_haves:
  truths: ["Reset and full seed reproduce the P-1042 aggregate.", "Five logical ticks reproduce the exact resolved fictional deterioration sequence."]
  artifacts: ["backend/app/vitals/scenario.py", "backend/app/vitals/service.py", "backend/app/seed/reset.py"]
  key_links: ["scenario tick -> service persistence", "reset -> full seed -> repeatable P-1042 aggregate"]
---
<objective>Expand the minimal fixture into a resettable full P-1042 aggregate and exact five-tick backend scenario.</objective>
<execution_context>@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-RESEARCH.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-01-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Implement full seed and explicit reset</name>
<files>backend/app/seed/reset.py, backend/app/seed/demo_data.py, backend/tests/test_seed.py</files>
<behavior><p>Test 1: Full seed has one stable P-1042 patient, admission, bed, nurse, history, and configuration aggregate.</p><p>Test 2: Reset then reseed and repeated seed preserve counts and IDs.</p></behavior>
<action>Extend the Plan 01 minimal fixture into an idempotent full seed/reset command with fictional values, stable external IDs, and resolved 5/10/30/manual configuration. Keep seed separate from migration execution and never insert MIMIC rows.</action>
<verify><automated>python -m pytest backend/tests/test_seed.py -q</automated></verify>
<done>Clean, repeated, and reset setup reconstructs the same P-1042 graph.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Implement the deterministic five-tick scenario</name>
<files>backend/app/vitals/scenario.py, backend/app/vitals/service.py, backend/tests/test_scenario.py</files>
<behavior><p>Test 1: ticks 0 through 4 equal the exact `p1042-deterioration-v1` values in 01-RESEARCH.md.</p><p>Test 2: reset returns tick 0 and bounded advancement rejects unbounded input.</p></behavior>
<action>Implement pure scenario calculation with seed `p1042-demo`, injected logical time, and immutable observation persistence through short-lived sessions. Use no browser time, wall-clock randomness, or clinical interpretation.</action>
<verify><automated>python -m pytest backend/tests/test_scenario.py -q</automated></verify>
<done>The backend scenario is exact, versioned, bounded, and reproducible without sleeping.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Scenario to persistence | Generated values become durable evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-02 | Tampering | Scenario advancement | high | mitigate | Bound ticks and validate server-owned scenario identity. |
| T-01-03 | Repudiation | Seed/reset | medium | mitigate | Stable IDs and repeat/reset tests preserve reconstruction. |
</threat_model>
<verification><automated>python -m pytest backend/tests/test_seed.py backend/tests/test_scenario.py -q</automated></verification>
<success_criteria>Full seed/reset and the resolved deterministic scenario are independently executable.</success_criteria>
<output>Create `.planning/phases/01-safety-simulation-and-backend-contracts/01-02-SUMMARY.md` when done.</output>
