---
phase: 01-safety-simulation-and-backend-contracts
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: [frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/api/client.ts, frontend/src/contracts/patients.ts, frontend/src/contracts/vitals.ts, frontend/src/monitoring/MonitoringPage.tsx, frontend/src/monitoring/MonitoringPage.test.tsx]
autonomous: true
requirements: [VITAL-03, SAFE-01]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 1, confidence: low}
must_haves:
  truths: ["The monitoring route renders P-1042 current state with typed vitals and server metadata.", "Focused frontend tests prove safety and degraded-state rendering."]
  artifacts: ["frontend/src/monitoring/MonitoringPage.tsx", "frontend/src/monitoring/MonitoringPage.test.tsx"]
  key_links: ["typed REST client -> monitoring page", "server freshness/provenance -> visible status and label"]
---
<objective>Bootstrap the typed React/Vite frontend and build the monitoring route and safety-state rendering against completed backend contracts.</objective>
<execution_context>@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-RESEARCH.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-03-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Render typed monitoring and safety states</name>
<files>frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/api/client.ts, frontend/src/contracts/patients.ts, frontend/src/contracts/vitals.ts, frontend/src/monitoring/MonitoringPage.tsx, frontend/src/monitoring/MonitoringPage.test.tsx</files>
<behavior><p>Test 1: P-1042 bed, six vitals, units, timestamps, sequence, provenance, freshness, and exact label render.</p><p>Test 2: fresh, stale, disconnected, and unavailable fixtures render distinct text without bedside or treatment claims.</p></behavior>
<action>Before installing the selected frontend packages, run and record concrete checks: `npm view react version`, `npm view vite version`, `npm view vitest version`, `npm view @testing-library/react version`, and `npm view @tanstack/react-query version`; a non-blocking check is allowed only when its output is recorded in the implementation summary. Bootstrap the React 19/Vite/TypeScript testable frontend in the listed package and config files, then build the route and typed REST client. Render server metadata without inventing freshness or provenance. Display exact wording `Simulated ICU environment - research prototype - not for clinical use` and retain public read-only local-prototype framing.</action>
<verify><automated>npm --prefix frontend run test -- --run src/monitoring/MonitoringPage.test.tsx</automated></verify>
<done>The route renders real contract data and honest safety/degraded states.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API response to viewer | Safety metadata controls interpretation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-05 | Spoofing | Monitoring presentation | high | mitigate | Render server-owned provenance and required label. |
</threat_model>
<verification><automated>npm --prefix frontend run test -- --run src/monitoring/MonitoringPage.test.tsx</automated></verification>
<success_criteria>The browser renders a truthful typed monitoring view with no clinical framing.</success_criteria>
<output>Create `.planning/phases/01-safety-simulation-and-backend-contracts/01-04-SUMMARY.md` when done.</output>
