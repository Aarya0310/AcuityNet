---
phase: 01-safety-simulation-and-backend-contracts
plan: 06
type: execute
wave: 6
depends_on: [01-05]
files_modified: [backend/app/safety/labels.py, backend/app/contracts/metadata.py, backend/app/transport/health.py, backend/tests/test_migrations.py, backend/tests/test_safety_boundary.py, scripts/phase1_smoke.py, README.md, .env.example]
autonomous: true
requirements: []
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 2, confidence: low}
must_haves:
  truths: ["Safety metadata and migration/reset checks are executable.", "README provides a runnable Windows full-stack smoke sequence."]
  artifacts: ["README.md", "backend/tests/test_safety_boundary.py", "backend/tests/test_migrations.py"]
  key_links: ["central label -> health/current responses", "README -> Uvicorn health/current-vitals and frontend build/dev checks"]
---
<objective>Close Phase 1 with centralized safety regression coverage and a directly runnable clean-reset/full-stack smoke strategy.</objective>
<execution_context>@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-RESEARCH.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-05-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add safety and migration regression checks</name>
<files>backend/app/safety/labels.py, backend/app/contracts/metadata.py, backend/app/transport/health.py, backend/tests/test_migrations.py, backend/tests/test_safety_boundary.py</files>
<behavior><p>Test 1: Health/current responses include centralized synthetic/prototype metadata.</p><p>Test 2: Retrospective or bedside provenance is rejected and fresh migration/foreign keys/seed separation are verified.</p></behavior>
<action>Centralize the exact safety label and require it in health/current metadata. Add migration and safety tests with fictional data only. Preserve public read-only synthetic Phase 1 scope and later JWT handoff.</action>
<verify><automated>python -m pytest backend/tests/test_migrations.py backend/tests/test_safety_boundary.py -q; cd backend; alembic --config alembic.ini check</automated></verify>
<done>Safety and schema boundaries are executable regressions.</done>
</task>
<task type="auto">
<name>Task 2: Document the clean full-stack smoke sequence</name>
<files>scripts/phase1_smoke.py, README.md, .env.example</files>
<action>Implement `scripts/phase1_smoke.py` as a checked-in deterministic API smoke runner using only the Python standard library for its HTTP assertions. It must launch Uvicorn as a child process, wait for `/health`, call `/health` and `/api/v1/patients/P-1042/vitals/current`, assert the exact synthetic provenance and a supported freshness value, and terminate the child process in a `finally` cleanup path. Document Windows PowerShell commands for migration, full seed, explicit reset, API start, frontend start/build, and tests, including `cd backend; alembic --config alembic.ini upgrade head` and `cd backend; alembic --config alembic.ini check`. State that Phase 1 reads are public synthetic research-prototype views, bounded advance is a fixture operation, and authenticated mutations begin later.</action>
<verify><automated>cd backend; alembic --config alembic.ini upgrade head; alembic --config alembic.ini check; cd ..; python scripts/phase1_smoke.py; python -m pytest backend/tests/test_migrations.py backend/tests/test_safety_boundary.py backend/tests/test_seed.py backend/tests/test_scenario.py backend/tests/test_vital_contracts.py backend/tests/test_vitals_api.py -q; npm --prefix frontend run test -- --run; npm --prefix frontend run build; npm --prefix frontend run lint</automated></verify>
<done>The checked-in smoke script starts and tears down the API, verifies both required endpoints plus synthetic provenance and freshness, and README contains the matching clean-reset and frontend commands.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API metadata to human | Labels determine interpretation. |
| Setup commands to SQLite | Ordering determines reproducibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-06 | Spoofing | Safety metadata | high | mitigate | Central server-owned label/provenance and regression tests. |
| T-01-07 | Repudiation | Reset workflow | medium | mitigate | Separate migration/seed/reset commands and tests. |
</threat_model>
<verification><automated>Run `cd backend; alembic --config alembic.ini upgrade head; alembic --config alembic.ini check; cd ..`, then `python scripts/phase1_smoke.py`, the full Phase 1 backend subset, `npm --prefix frontend run test -- --run`, frontend build, and lint commands.</automated></verification>
<success_criteria>Phase 1 has executable safety, reset, documentation, and full-stack smoke evidence.</success_criteria>
<output>Create `.planning/phases/01-safety-simulation-and-backend-contracts/01-06-SUMMARY.md` when done.</output>