---
phase: 03-monitoring-alerts-lifecycle-and-audit
plan: 04
date: 2026-08-24
type: execute
wave: 4
depends_on: [03-03]
files_modified: [backend/app/seed/reset.py, backend/app/seed/demo_data.py, backend/tests/test_phase3_integration.py, backend/tests/test_phase3_migration.py, scripts/phase3_smoke.py, README.md]
autonomous: true
requirements: [ALRT-01, ALRT-02, ALRT-03, ALRT-04, ALRT-05, AUDT-01, REAL-01, REAL-02]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: low}
user_setup:
  - service: phase2_dependencies
    why: "Phase 2 recorded pytest and frontend dependency verification blockers; Phase 3 execution must install/verify the already-declared dependencies before its focused suites can be evidence."
must_haves:
  truths:
    - "Reset and reseed remove alert/lifecycle/audit rows in dependency-safe order and restore the deterministic P-1042 demonstration without schema changes in seed code."
    - "One real integration path proves deterioration, prediction/configuration evidence, alert deduplication, valid lifecycle, ordered audit reconstruction, authorization denials, and REST recovery claims without leaking secrets."
    - "The Phase 3 smoke command is reproducible on a clean SQLite database and states Phase 4 dispatch/historian/nurse behavior as intentionally absent rather than implying it exists."
  artifacts:
    - path: "backend/tests/test_phase3_integration.py"
      provides: "Cross-layer P-1042 alert/lifecycle/audit/recovery integration coverage"
    - path: "scripts/phase3_smoke.py"
      provides: "Secret-safe child-process smoke journey"
    - path: "backend/app/seed/reset.py"
      provides: "Alert/event/audit-aware dependency deletion ordering"
  key_links:
    - from: "reset_demo_data"
      to: "AlertEvent/AuditEvent/Alert"
      via: "children deleted before alert and existing fixture rows"
    - from: "phase3_smoke.py"
      to: "Uvicorn REST/WebSocket app"
      via: "child environment secret preflight and HTTP assertions"
    - from: "clean SQLite migration/seed"
      to: "P-1042 lifecycle/audit reconstruction"
      via: "integration test and documented reset/reseed path"
---
<objective>Close Phase 3 with clean-fixture reproducibility and one executable cross-layer proof of monitoring, alert, lifecycle, audit, and realtime recovery boundaries.</objective>

Purpose: Convert individually tested contracts into evidence that the requested mentor journey is reproducible without claiming Phase 4 capabilities.
Output: Reset/reseed support, integration/concurrency regressions, secret-safe smoke runner, and dated README verification instructions.

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-RESEARCH.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-PATTERNS.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-01-SUMMARY.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-02-SUMMARY.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-03-SUMMARY.md
@backend/app/seed/reset.py
@backend/app/seed/demo_data.py
@backend/tests/test_phase2_integration.py
@scripts/phase2_smoke.py
@README.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Wire clean reset/reseed through the complete Phase 3 integration journey</name>
  <files>backend/app/seed/reset.py, backend/app/seed/demo_data.py, backend/tests/test_phase3_integration.py, backend/tests/test_phase3_migration.py, scripts/phase3_smoke.py, README.md</files>
  <action>Follow the Phase 2 smoke and seed/reset analogs. Extend reset_demo_data to delete audit/lifecycle children before alerts and preserve existing dependency order; keep migration execution separate from idempotent seeding. Seed only the three demo accounts and deterministic P-1042 configuration, with no fabricated alert or assignment. Add real create_app/TestClient integration coverage that logs in U-ADMIN, U-DOCTOR, U-SARAH, and U-ALEX, advances exact ticks, proves threshold/fallback provenance, creates one alert, repeats generation, exercises the exact lifecycle DTO/routes and role matrix, fetches ordered audit evidence including configuration and denials, and verifies REST reads after the real socket closes. Extend scripts/phase3_smoke.py with the Phase 2 child-process pattern and secret-safe output. Document PowerShell commands, expected states, no-candidate typed-only behavior, and prototype limitations dated 2026-08-24. Do not add historian, candidate ranking, human confirmation/override, fabricated no-candidate decisions, assigned-Nurse UX, or other Phase 4 behavior.</action>
  <verify><automated>python -m pytest backend/tests/test_phase3_migration.py backend/tests/test_phase3_integration.py -q
python scripts/phase3_smoke.py</automated></verify>
  <done>A clean local database can reproduce the Phase 3 alert-to-audit journey and REST recovery with secret-safe executable evidence.</done>
</task>
<task type="auto">
  <name>Task 2: Expand cross-layer regression gates and reset integrity</name>
  <files>backend/app/seed/reset.py, backend/app/seed/demo_data.py, backend/tests/test_phase3_integration.py, backend/tests/test_phase3_migration.py, scripts/phase3_smoke.py, README.md</files>
  <action>Use real temporary SQLite migrations and fixed clocks to verify downgrade ordering, foreign keys, indexes/uniqueness, reset idempotence, reseed counts, repeated smoke execution, concurrent evaluation/lifecycle conflict behavior, equal-timestamp ordering, configuration/anonymous/authenticated denial audit entries, malformed realtime recovery, and secret-safe output. Assert alert KPI values are backed by Phase 3 rows while response metrics remain not_yet_available for Phase 4. Keep failure output actionable and never include passwords, JWTs, Authorization headers, or secret values. Treat the Phase 2 pytest/frontend installation gate as a precondition; if it remains unmet, record the blocked suites explicitly rather than claiming a pass.</action>
  <verify><automated>python -m pytest backend/tests/test_phase3_migration.py backend/tests/test_phase3_integration.py backend/tests/test_alerts.py backend/tests/test_lifecycle_audit.py backend/tests/test_realtime.py -q
python scripts/phase3_smoke.py</automated></verify>
  <done>Phase 3’s migration/reset, concurrency, lifecycle/audit, recovery, and reproducibility risks have executable regression coverage and documented clean-run commands.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| setup/reset command -> SQLite file | Destructive development fixture operations cross into durable data. |
| smoke child process -> HTTP/WebSocket app | Test credentials and secret environment cross a process boundary. |
| integration output -> developer terminal/docs | Failure/reporting paths can accidentally disclose credentials or tokens. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-12 | Tampering | Reset/reseed dependency graph | high | mitigate | Delete child events before parents, run real FK-enabled SQLite tests, and keep migration separate from seed. |
| T-03-13 | Information disclosure | Smoke runner/output | high | mitigate | Preflight secret presence, pass secret only to child environment, redact credentials/tokens/headers, and assert output safety. |
| T-03-14 | Denial of service | Repeated/concurrent demo execution | medium | mitigate | Use isolated temporary databases, bounded deterministic ticks, uniqueness constraints, and cleanup in finally paths. |
| T-03-SC | Tampering | Existing package/runtime dependencies | high | mitigate | No new dependency install; execute against the audited Phase 2 stack and document prerequisite installation separately. |
</threat_model>

<verification>
<automated>python -m pytest backend/tests/test_phase3_migration.py backend/tests/test_phase3_integration.py backend/tests/test_alerts.py backend/tests/test_lifecycle_audit.py backend/tests/test_realtime.py -q
python scripts/phase3_smoke.py</automated>
</verification>

<success_criteria>All Phase 3 requirements have a clean SQLite integration path, migration/reset/reseed evidence, concurrency coverage, and a secret-safe runnable smoke command, while Phase 4 remains visibly out of scope.</success_criteria>

<output>Create .planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-04-SUMMARY.md when done.</output>
