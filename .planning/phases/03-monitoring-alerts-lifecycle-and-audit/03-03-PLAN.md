---
phase: 03-monitoring-alerts-lifecycle-and-audit
plan: 03
date: 2026-08-24
type: execute
wave: 3
depends_on: [03-02]
files_modified: [backend/app/contracts/realtime.py, backend/app/transport/realtime.py, backend/app/main.py, frontend/src/contracts/alerts.ts, frontend/src/contracts/realtime.ts, frontend/src/api/client.ts, frontend/src/alerts/AlertPage.tsx, frontend/src/alerts/useAlertRealtime.ts, frontend/src/operational/OperationalState.tsx, frontend/src/monitoring/MonitoringPage.tsx, frontend/src/App.tsx, frontend/src/alerts/AlertPage.test.tsx]
autonomous: true
requirements: [REAL-01, REAL-02]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "REST supplies authoritative alert, lifecycle, audit, prediction, and vital state on initial load, reload, mutation, reconnect, and missed WebSocket messages."
    - "WebSocket delivery is authenticated, patient-scoped, best-effort invalidation only, and exposes connecting/connected/disconnected/error state without becoming a second read or mutation API."
    - "The UI visibly distinguishes loading, stale, disconnected, unavailable, deterministic fallback, and typed no-candidate states; old data never looks current after a failed refresh."
  artifacts:
    - path: "backend/app/transport/realtime.py"
      provides: "Authenticated minimal invalidation WebSocket endpoint and disconnect handling"
    - path: "frontend/src/alerts/useAlertRealtime.ts"
      provides: "Bounded reconnect/cleanup hook that invalidates and refetches REST queries"
    - path: "frontend/src/operational/OperationalState.tsx"
      provides: "Explicit operational-state presentation"
    - path: "frontend/src/alerts/AlertPage.tsx"
      provides: "Current alert, evidence, action states, provenance, fallback, and lifecycle view"
    - path: "frontend/src/alerts/AlertPage.test.tsx"
      provides: "REST recovery, socket close, malformed message, and operational-state UI coverage"
  key_links:
    - from: "REST alert mutation commit"
      to: "WebSocket invalidation envelope"
      via: "in-process best-effort publisher with resource identity only"
    - from: "useAlertRealtime"
      to: "TanStack Query cache"
      via: "invalidate/refetch alert, audit, prediction, and current-vitals keys"
    - from: "AlertPage/MonitoringPage"
      to: "api/client.ts"
      via: "centralized bearer-aware REST calls and explicit error state"
---
<objective>Make alert and monitoring evidence usable during realtime disruption by adding additive WebSocket invalidation and explicit browser operational states over REST-authoritative queries.</objective>

Purpose: A socket outage must change the displayed operational status, not the truth source or the operator’s ability to recover state.
Output: Authenticated invalidation channel, reconnect/refetch hook, typed alert/realtime contracts, alert evidence UI, and focused frontend recovery tests.

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-RESEARCH.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-PATTERNS.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-02-SUMMARY.md
@backend/app/main.py
@backend/app/auth/policy.py
@backend/app/contracts/vitals.py
@backend/app/contracts/predictions.py
@backend/app/transport/alerts.py
@frontend/src/main.tsx
@frontend/src/App.tsx
@frontend/src/api/client.ts
@frontend/src/monitoring/MonitoringPage.tsx
@frontend/src/monitoring/MonitoringPage.test.tsx
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Wire REST alert evidence through an authenticated invalidation channel to recovery UI</name>
  <files>backend/app/contracts/realtime.py, backend/app/transport/realtime.py, backend/app/main.py, frontend/src/contracts/alerts.ts, frontend/src/contracts/realtime.ts, frontend/src/api/client.ts, frontend/src/alerts/AlertPage.tsx, frontend/src/alerts/useAlertRealtime.ts, frontend/src/operational/OperationalState.tsx, frontend/src/monitoring/MonitoringPage.tsx, frontend/src/App.tsx, frontend/src/alerts/AlertPage.test.tsx</files>
  <action>Follow PATTERNS.md realtime and frontend query analogs. Define closed notification and operational-state contracts; include patient/alert identity and server event ID but no authoritative clinical payload. Add an authenticated FastAPI WebSocket route with existing bearer identity/resource policy, patient scoping, malformed-message/disconnect handling, and no mutation/read semantics. Publish only after committed REST observation/alert mutations through the injected app seam. Extend api/client.ts with typed current-alert, lifecycle, audit, and REST recovery methods while preserving centralized bearer headers, 401 clearing, and non-2xx errors. Build AlertPage with TanStack Query keys for current alert and ordered evidence, visible priority/risk/event/probability/horizon/bed/provenance/source/fallback/lifecycle, and explicit loading/error/empty branches. Add useAlertRealtime with cleanup, bounded reconnect backoff, logout stop, socket-state display, and invalidate/refetch on valid hints or reconnect. Update MonitoringPage/App to stop swallowing failed reads and to show server freshness plus disconnected/stale state. Keep no-candidate as a typed empty operational result for future dispatch compatibility; do not create Phase 4 candidate logic. Test the single end-to-end browser path against mocked network/socket boundaries and a real backend contract shape.</action>
  <verify><automated>npm --prefix frontend run test -- --run src/alerts/AlertPage.test.tsx; npm --prefix frontend run build; python -m pytest backend/tests/test_lifecycle_audit.py -q</automated></verify>
  <done>A user can load current alert evidence from REST, see honest realtime connection status, lose the socket, and recover current alert/audit state through REST without local clinical-state arithmetic.</done>
</task>
<task type="auto">
  <name>Task 2: Expand degraded-state and REST-authority coverage</name>
  <files>backend/app/contracts/realtime.py, backend/app/transport/realtime.py, frontend/src/contracts/alerts.ts, frontend/src/contracts/realtime.ts, frontend/src/api/client.ts, frontend/src/alerts/AlertPage.tsx, frontend/src/alerts/useAlertRealtime.ts, frontend/src/operational/OperationalState.tsx, frontend/src/monitoring/MonitoringPage.tsx, frontend/src/alerts/AlertPage.test.tsx</files>
  <action>Use MonitoringPage.test.tsx conventions with fake timers and semantic queries. Cover loading, stale server response, failed current/alert/audit fetch, 401/403, unavailable data, deterministic_fallback with reason, malformed or out-of-scope socket messages, close/error, bounded reconnect, unmount cleanup, reconnect-triggered REST refetch, and page reload starting from REST. Assert the last successful value is retained only with a prominent stale/disconnected indicator. Keep safety/prototype metadata visible and do not imply diagnosis, bedside truth, validated weights, autonomous assignment, or a fabricated no-candidate result.</action>
  <verify><automated>npm --prefix frontend run test -- --run src/alerts/AlertPage.test.tsx src/monitoring/MonitoringPage.test.tsx; npm --prefix frontend run lint; npm --prefix frontend run build</automated></verify>
  <done>REAL-01 and REAL-02 have explicit frontend and transport tests for recovery and every required operational state.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| browser -> WebSocket handshake | Client transport attempts to subscribe to patient-scoped invalidations. |
| WebSocket message -> query cache | Untrusted notification data may influence refetch behavior. |
| browser -> REST alert/audit reads and commands | UI requests authoritative scoped resources and mutations. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-08 | Spoofing | WebSocket handshake | high | mitigate | Reuse bearer identity and patient/resource policy; close unauthorized or malformed subscriptions. |
| T-03-09 | Tampering | Realtime envelope/client state | high | mitigate | Treat socket payload as invalidation identity only; refetch authoritative REST contracts and validate message shape. |
| T-03-10 | Information disclosure | Alert/audit UI and socket scope | high | mitigate | Scope subscription and REST queries through existing role/resource policy; retain no secret payloads. |
| T-03-11 | Denial of service | Reconnect loop | medium | mitigate | Bound backoff, stop on unmount/logout, and never open a persistent session-level database connection. |
| T-03-SC | Tampering | Existing package/runtime dependencies | high | mitigate | Use FastAPI/Starlette and installed TanStack Query only; no new package install. |
</threat_model>

<verification>
<automated>npm --prefix frontend run test -- --run src/alerts/AlertPage.test.tsx src/monitoring/MonitoringPage.test.tsx; npm --prefix frontend run lint; npm --prefix frontend run build</automated>
</verification>

<success_criteria>REST recovery remains authoritative after socket loss/reload, and all required loading/freshness/transport/fallback/empty states are truthful and test-covered.</success_criteria>

<output>Create .planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-03-SUMMARY.md when done.</output>
