---
phase: 04-medical-historian-and-human-confirmed-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/persistence/models.py
  - backend/app/migrations/versions/0004_historian_context.py
  - backend/app/contracts/historian.py
  - backend/app/historian/service.py
  - backend/app/transport/historian.py
  - backend/app/main.py
  - backend/app/seed/demo_data.py
  - backend/tests/test_historian.py
  - backend/tests/test_phase4_migration.py
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03]
user_setup: []
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The protected historian REST projection returns the complete seeded P-1042 clinical-context record, current prediction, alert evidence, notes, and ordered journey timeline."
    - "The historian projection separates immutable baseline risk from contextual risk, names every contributing fact and configurable rule delta, and labels the result as a research prototype explanation."
    - "Incomplete context produces baseline-only output with contextual risk unavailable and explicit missing evidence; annotations appear on the timeline without changing scores."
    - "Admin and Doctor historian reads and Doctor annotation writes are authorized server-side, while Nurse access is denied."
  artifacts:
    - path: "backend/app/migrations/versions/0004_historian_context.py"
      provides: "Migration-backed context facts, named rules/evaluations, and timeline annotation persistence"
    - path: "backend/app/historian/service.py"
      provides: "Server-owned completeness gate, rule evaluation, immutable historian projection, and timeline merge"
    - path: "backend/app/transport/historian.py"
      provides: "Protected historian and Doctor annotation REST endpoints"
  key_links:
    - from: "backend/app/historian/service.py"
      to: "backend/app/persistence/models.py"
      via: "queries typed patient facts/rules and persisted evaluation snapshots without querying annotations for scoring"
    - from: "backend/app/transport/historian.py"
      to: "backend/app/auth/policy.py"
      via: "server-side Admin/Doctor read and Doctor-only annotation authorization"
---

<objective>
Deliver the Medical Historian backend persistence and service contract for P-1042.

Purpose: The historian backend needs an explainable contextual research projection grounded in complete seeded facts while preserving the existing prediction, alert, audit, and safety contracts.
Output: Additive migration/models, deterministic historian seed, typed REST contracts and service, Doctor annotation endpoint, and focused backend tests.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-medical-historian-and-human-confirmed-dispatch/04-CONTEXT.md
@.planning/phases/04-medical-historian-and-human-confirmed-dispatch/04-RESEARCH.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-02-SUMMARY.md
@backend/app/persistence/models.py
@backend/app/auth/policy.py
@backend/app/audit/service.py
@backend/app/transport/alerts.py
@backend/app/main.py
@backend/app/seed/demo_data.py
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace complete historian evidence from SQLite through the backend REST timeline projection</name>
  <files>backend/app/persistence/models.py, backend/app/migrations/versions/0004_historian_context.py, backend/app/contracts/historian.py, backend/app/historian/service.py, backend/app/transport/historian.py, backend/app/main.py, backend/app/seed/demo_data.py, backend/tests/test_historian.py</files>
  <action>Implement one production-quality complete-context backend path for P-1042 with typed facts, named versioned rules, immutable evaluations, protected GET historian and POST annotation routes, and ordered alert/audit timeline data. Apply D-01, D-02, D-04, D-05, D-06, and D-07. Use the resolved fictional fixture values and 86400-second prototype context freshness window from 04-RESEARCH.md; all four categories contribute through named rules, annotations never alter scores, incomplete context is baseline-only, and synthetic/non-clinical provenance is mandatory.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_historian.py -q</automated>
  </verify>
  <done>A seeded authorized request returns a complete historian response with timeline, baseline/contextual explanation, named rules, prototype label, and annotation entry contract; Admin can read it, Nurse cannot use the Doctor-only annotation mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove historian incompleteness, annotation immutability, migration, and access boundaries</name>
  <files>backend/tests/test_historian.py, backend/tests/test_phase4_migration.py</files>
  <behavior>
    - Complete P-1042 context includes all four categories, four named deltas, empty missing evidence, and contextual score equal to baseline plus all deltas within bounds.
    - Removing one required category returns baseline score, null contextual score, incomplete status, named missing evidence, and no partial delta total.
    - A Doctor annotation is timestamped and audited without changing scores; Admin cannot create it and Nurse cannot access the unrestricted historian.
    - Fresh migration exposes the historian tables and exact seeded context.
  </behavior>
  <action>Expand backend tests for exact fixture IDs/values, completeness and freshness, D-03 annotation immutability, D-04 baseline-only behavior, D-06 audit evidence, closed DTOs, role/resource denials, migration indexes/foreign keys, unpaginated response, and secret-safe bounded details.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_historian.py backend/tests/test_phase4_migration.py -q</automated>
  </verify>
  <done>Historian completeness, baseline-only fallback, Doctor annotation semantics, migration integrity, reset expectations, and role boundaries are executable backend regression checks.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doctor/Admin/Nurse client -> historian REST | Authenticated users request patient clinical-context projections and annotation mutations. |
| Seeded context storage -> historian evaluator | Persisted synthetic facts and configurable rule definitions become explanation inputs. |
| Historian/annotation service -> audit storage | User-authored annotations and denied actions become append-only evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | historian transport and policy | high | mitigate | Enforce Admin/Doctor reads, Doctor-only annotation creation, and patient checks in the API; test bypass attempts. |
| T-04-02 | Tampering | historian evaluator | high | mitigate | Keep rule inputs server-owned, annotations excluded from scoring, complete-category gate mandatory, and evaluations tied to prediction evidence. |
| T-04-03 | Information Disclosure | historian and audit DTOs | medium | mitigate | Return only permitted projections, use bounded closed contracts, preserve synthetic provenance, and assert audit details contain no credentials/tokens. |
| T-04-04 | Spoofing/Repudiation | timeline annotations | medium | mitigate | Persist actor/time and append annotation audit evidence with stable ordering. |
| T-04-SC | Tampering | package installs | high | accept | No package installation is planned; existing pinned dependencies are reused. |
</threat_model>

<verification>
Run the focused historian backend and migration tests, plus the existing Phase 3 lifecycle/audit regression suite. Confirm the protected REST projection keeps the prototype label and bounded evidence visible.
</verification>

<success_criteria>
HIST-01 through HIST-03 are owned exactly by this plan and pass through a migrated, seeded, authorized REST response with complete/incomplete context and annotation audit behavior.
</success_criteria>

<output>
Create `.planning/phases/04-medical-historian-and-human-confirmed-dispatch/04-01-SUMMARY.md` when done.
</output>
