---
phase: 04-medical-historian-and-human-confirmed-dispatch
plan: 06
type: execute
wave: 5
depends_on: [04-02, 04-04, 04-05]
files_modified:
  - backend/app/seed/reset.py
  - backend/app/seed/demo_data.py
  - backend/tests/test_phase4_migration.py
  - backend/tests/test_phase4_integration.py
  - scripts/phase4_smoke.py
  - README.md
autonomous: true
requirements: []
user_setup:
  - service: local environment
    why: "The isolated smoke run requires a local JWT secret."
    env_vars:
      - name: ACUITYNET_JWT_SECRET
        source: "Generate a local development-only secret before running the documented smoke command."
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Reset deletes Phase 4 children before Phase 3 parents and reseed restores deterministic context, rules, and neutral candidate inputs with exactly three users and no alert/assignment artifacts."
    - "An isolated executable replay proves historian, ranked evaluation, human confirmation, Nurse lifecycle, ordered audit, and no-candidate preservation."
    - "Smoke output is reproducible and contains no credentials, bearer values, or authorization headers."
  artifacts:
    - path: "backend/app/seed/reset.py"
      provides: "Foreign-key-safe Phase 4 reset ordering"
    - path: "scripts/phase4_smoke.py"
      provides: "Temporary SQLite full journey evidence"
    - path: "backend/tests/test_phase4_integration.py"
      provides: "Cross-layer replay and blocked-path assertions"
  key_links:
    - from: "backend/app/seed/reset.py"
      to: "backend/app/persistence/models.py"
      via: "Phase 4 children are deleted before existing alert, audit, prediction, and fixture parents"
    - from: "scripts/phase4_smoke.py"
      to: "backend/app/main.py"
      via: "Temporary database URL and secret-safe Uvicorn/TestClient journey"
---

<objective>Close Phase 4 with dependency-safe reset/reseed and executable isolated end-to-end evidence.</objective>
<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-medical-historian-and-human-confirmed-dispatch/04-CONTEXT.md
@.planning/phases/04-medical-historian-and-human-confirmed-dispatch/04-RESEARCH.md
@.planning/phases/03-monitoring-alerts-lifecycle-and-audit/03-04-SUMMARY.md
@backend/app/seed/reset.py
@backend/app/seed/demo_data.py
@backend/tests/test_phase3_integration.py
@README.md
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Trace reset and isolated full P-1042 replay</name>
  <files>backend/app/seed/reset.py, backend/app/seed/demo_data.py, backend/tests/test_phase4_migration.py, backend/tests/test_phase4_integration.py, scripts/phase4_smoke.py, README.md</files>
  <action>Extend reset in child-first order: annotations, decisions, candidates, evaluations, rule evaluations, context facts, then existing alert events/audit/alerts/predictions/observations and parents. Reseed the exact four fictional context facts, rules.v1, Sarah candidate inputs, prototype freshness configuration, exactly three accounts, and no alert/assignment/decision/retry artifacts. Add an isolated temporary SQLite E2E path that advances deterioration, reads historian, evaluates dispatch, exercises stale/no-candidate preservation, confirms with Admin/Doctor reason, runs Sarah acknowledge/respond/resolve, reconstructs timeline/audit, and proves no autonomous assignment. Document the local secret preflight and expected safe output.</action>
  <verify><automated>python -m pytest backend/tests/test_phase4_integration.py backend/tests/test_phase4_migration.py -q && python scripts/phase4_smoke.py</automated></verify>
  <done>Reset/reseed is foreign-key safe and one isolated smoke run reproduces the complete Phase 4 journey plus blocked no-candidate behavior without modifying the developer database.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove clean replay, exact seed boundary, and audit safety</name>
  <files>backend/tests/test_phase4_integration.py, backend/tests/test_phase4_migration.py, scripts/phase4_smoke.py</files>
  <behavior>
    - Fresh migration exposes Phase 4 tables/indexes; reset and reseed restore exact three accounts and neutral pre-alert state.
    - Full replay preserves ordered historian/dispatch/lifecycle/audit evidence and no-candidate leaves generated/unassigned.
    - Repeated smoke runs use isolated databases and never emit password, bearer, or authorization material.
  </behavior>
  <action>Expand integration, migration, and smoke assertions for D-16, D-17, and D-18, exact three-role boundary, clean reset ordering, stale conflict non-mutation, audit detail bounds, deterministic rerun, and compatibility with Phase 3 evidence. Do not claim additional requirement ownership; this plan is verification expansion for the owning plans.</action>
  <verify><automated>python -m pytest backend/tests/test_phase4_integration.py backend/tests/test_phase4_migration.py backend/tests/test_historian.py backend/tests/test_dispatch.py backend/tests/test_nurse_workflow.py -q && python scripts/phase4_smoke.py</automated></verify>
  <done>Phase 4 reset, integration, no-candidate, stale, security, and reproducibility evidence is executable and documented.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Reset/smoke process -> SQLite | Automation creates and deletes synthetic workflow records. |
| Smoke process -> authenticated app | Test credentials and bearer sessions are used for local verification. |
| Full journey -> audit evidence | All decisions and lifecycle actions become durable reconstruction data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-14 | Tampering | reset ordering | high | mitigate | Delete dependent Phase 4 rows first and assert foreign-key-safe reset/reseed. |
| T-04-15 | Information Disclosure | smoke output/audit | high | mitigate | Use temporary DB, preflight secret, bounded audit assertions, and output redaction tests. |
| T-04-16 | Repudiation | E2E evidence | medium | mitigate | Assert ordered stable-ID timeline, decision snapshot, lifecycle.assign, and denial evidence. |
| T-04-SC | Tampering | package installs | high | accept | No package installation; reuse pinned dependencies. |
</threat_model>
<verification>Run the full Phase 4 backend suite and isolated smoke twice; run the relevant frontend suites and lint from plans 02, 04, and 05.</verification>
<success_criteria>Phase 4 can be cleanly reset and replayed end to end with all locked safety, role, audit, freshness, and non-autonomous dispatch boundaries proven.</success_criteria>
<output>Create `.planning/phases/04-medical-historian-and-human-confirmed-dispatch/04-06-SUMMARY.md` when done.</output>
