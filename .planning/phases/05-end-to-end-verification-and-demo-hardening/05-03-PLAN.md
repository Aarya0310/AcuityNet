---
phase: 05-end-to-end-verification-and-demo-hardening
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/src/monitoring/MonitoringPage.test.tsx
  - frontend/src/alerts/AlertPage.test.tsx
  - frontend/src/historian/HistorianPage.test.tsx
  - frontend/src/dispatch/DispatchPage.test.tsx
  - frontend/src/hooks/useAlertRealtime.test.ts
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 20000
  raw_tokens: 10000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Frontend components render correctly with mocked API responses and handle loading/error states."
    - "Operational state badges (stale, fallback, denied, no-candidate) are visible with appropriate visual treatment (badge color, warning text)."
    - "Form submissions call correct API endpoints and handle success/error responses with toasts/modals."
    - "WebSocket realtime hook connects/disconnects gracefully and triggers invalidation/reconnect backoff."
  artifacts:
    - path: "frontend/src/monitoring/MonitoringPage.test.tsx"
      provides: "Vital display, stale badge, refresh action, synthetic label"
    - path: "frontend/src/alerts/AlertPage.test.tsx"
      provides: "Alert priority badge, lifecycle buttons, denied/no-candidate messaging"
    - path: "frontend/src/historian/HistorianPage.test.tsx"
      provides: "Rule explanation rendering, annotation form, timeline visualization"
    - path: "frontend/src/dispatch/DispatchPage.test.tsx"
      provides: "Candidate ranking display, confirmation form, no-candidate message"
    - path: "frontend/src/hooks/useAlertRealtime.test.ts"
      provides: "WebSocket connect/disconnect, invalidation handling, reconnect backoff"
  key_links:
    - from: "frontend/src/*/Page.test.tsx"
      to: "frontend/src/api/client.ts"
      via: "Fetch API mocked at component boundary; components assume stable response contracts"
    - from: "frontend/src/hooks/useAlertRealtime.test.ts"
      to: "backend/app/contracts/realtime.py"
      via: "WebSocket message format validated; disconnect triggers REST refetch"
---

<objective>
Verify frontend component layer handles all critical UI paths: vital display with staleness, alert lifecycle progression, historian context rendering, dispatch ranking and confirmation, and real-time WebSocket behavior.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-end-to-end-verification-and-demo-hardening/05-RESEARCH.md
@frontend/src/**/*.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Page component tests (monitoring, alerts, historian, dispatch)</name>
  <files>frontend/src/monitoring/MonitoringPage.test.tsx, frontend/src/alerts/AlertPage.test.tsx, frontend/src/historian/HistorianPage.test.tsx, frontend/src/dispatch/DispatchPage.test.tsx</files>
  
  <behavior>
    - **MonitoringPage**: Renders vital cards with current values, timestamp, stale badge (if >cooldown), "Simulated" label, refresh button; clicking refresh refetches vitals
    - **AlertPage**: Renders alert priority badge (high/critical color), lifecycle buttons (acknowledge/respond/resolve) enabled based on state, denied access shows generic "Access Denied" message, no-candidate shows "No nurses available" with retry button
    - **HistorianPage**: Renders demographics section, diagnoses/meds/labs lists, baseline vs contextual risk breakdown, rule explanation cards with delta values, timeline linking events, all labeled as "prototype rules"
    - **DispatchPage**: Renders candidate list with scores (availability %, proximity, workload %), top candidate highlighted, "Confirm" button, "Refresh Candidates" button, no-candidate shows empty list with message
  </behavior>

  <action>
Create or extend four Vitest test files following existing React Testing Library patterns (use render, screen, fireEvent, waitFor from @testing-library/react):

**frontend/src/monitoring/MonitoringPage.test.tsx:**
- Test 1: Renders with mock vital API response
  * Setup: Mock GET `/api/v1/patients/P-1042/vitals/current` → {SpO2: 92, HR: 85, ..., timestamp, freshness_state}
  * Render MonitoringPage
  * Assert vital cards display values (92% SpO2, 85 bpm HR, etc.)
  * Assert "Simulated ICU" or "Research Prototype" label visible
- Test 2: Displays stale badge when freshness > threshold
  * Mock vital with freshness_state="stale"
  * Render MonitoringPage
  * Assert "Stale" or "Last updated..." badge visible
  * Assert vital values still readable (not hidden)
- Test 3: Refresh button fetches fresh vitals
  * Mock initial vital response, then new response with different values
  * Render, click "Refresh" button
  * Assert new values displayed
  * Assert API called twice (before/after refresh)
- Test 4: Error state handling
  * Mock vital API 500 error
  * Render MonitoringPage
  * Assert error message displayed (generic, no PHI)
  * Assert retry button available

**frontend/src/alerts/AlertPage.test.tsx:**
- Test 1: Renders alert with priority badge
  * Mock GET alert → {priority: "critical", state: "generated", patient_id: "P-1042", bed: "ICU-12", ...}
  * Render AlertPage
  * Assert priority badge color (red for critical)
  * Assert alert metadata displayed (bed, patient, risk score)
- Test 2: Lifecycle buttons enabled based on state
  * Mock alert state="generated" → only "assign" or "acknowledge" button enabled
  * Render, assert button state
  * Mock alert state="acknowledged" → "respond" button enabled
  * Re-render, assert button state changes
  * Assert invalid transitions show disabled or hidden button
- Test 3: Lifecycle action calls correct API
  * Mock POST `/api/v1/patients/P-1042/alert/acknowledge` → {status: "acknowledged"}
  * Render, click "Acknowledge" button
  * Assert POST called with correct payload
  * Assert success toast shown
- Test 4: Denied access shows generic message
  * Mock alert API 403 response
  * Render AlertPage
  * Assert "Access Denied" message shown (not "You are not assigned")
  * Assert no patient details visible
- Test 5: No-candidate state shows message and retry
  * Mock alert state="generated", dispatch outcome="no_candidate"
  * Render AlertPage (or dispatch context)
  * Assert "No eligible nurses available" message
  * Assert "Retry Evaluation" button available
  * Click retry → POST evaluation again

**frontend/src/historian/HistorianPage.test.tsx:**
- Test 1: Renders complete historian data
  * Mock GET historian → {demographics, diagnoses, medications, labs, baseline_risk, contextual_risk, rules[...], timeline[...]}
  * Render HistorianPage
  * Assert demographics section shows patient name, DOB, admission date
  * Assert diagnoses list populated (non-empty)
  * Assert medications list populated
  * Assert labs table with values (glucose, potassium, etc.)
- Test 2: Risk breakdown (baseline vs contextual)
  * Mock historian with baseline_risk=0.30, contextual_risk=0.45, delta=0.15
  * Render HistorianPage
  * Assert baseline displayed ("30% from history")
  * Assert contextual displayed ("45% with current context")
  * Assert delta explained ("Rule X: +10% for high HR")
- Test 3: Rule explanation labeled as prototype
  * Mock rules array with descriptions
  * Render rule cards
  * Assert each rule labeled "Research Rule" or "Prototype Rule"
  * Assert rule name, factor, delta value visible
  * Assert NO language claiming validated clinical weighting (e.g., "evidence-based", "clinically validated")
- Test 4: Timeline links events
  * Mock timeline with events: [deterioration, prediction, alert, dispatch, acknowledge, respond, resolve]
  * Render timeline
  * Assert ≥6 events visible with timestamps
  * Assert events connected (visual line or spacing)
- Test 5: Error state handling
  * Mock historian API 500 error
  * Render HistorianPage
  * Assert error message shown
  * Assert retry button available

**frontend/src/dispatch/DispatchPage.test.tsx:**
- Test 1: Renders candidate ranking
  * Mock POST evaluation → {candidates: [{name: "Sarah", score: 0.85, scores: {availability: 0.40, proximity: 0.30, workload: 0.10, acuity: 0.05}}, {...}]}
  * Render DispatchPage
  * Assert candidates list rendered in descending score order
  * Assert Sarah at top
  * Assert score components displayed (40% availability, 30% proximity, etc.)
- Test 2: Confirm button and form
  * Mock evaluation response
  * Render DispatchPage
  * Click "Confirm" on top candidate
  * Assert confirmation form/modal appears
  * Assert can add reason/note
  * Mock POST confirm → {assignment: {id, nurse_id, alert_id}}
  * Submit form
  * Assert success toast
  * Assert page updated or redirected
- Test 3: Refresh candidates evaluates dispatch again
  * Mock evaluation response
  * Render, click "Refresh Candidates"
  * Assert POST evaluation called again
  * Assert new candidates displayed
- Test 4: No-candidate message and retry
  * Mock evaluation → {candidates: [], outcome: "no_candidate"}
  * Render DispatchPage
  * Assert "No eligible nurses available" message
  * Assert candidate list empty
  * Assert "Retry Evaluation" button
  * Click retry, assert POST evaluation called again
- Test 5: Fallback prediction indicator
  * Mock evaluation with prediction.source_kind="deterministic_fallback"
  * Render DispatchPage
  * Assert "Fallback Prediction" badge or label visible
  * Assert NO model name/version displayed
  </action>

  <verify>
    <automated>npm --prefix frontend run test -- --run MonitoringPage AlertPage HistorianPage DispatchPage</automated>
  </verify>

  <done>
    - All 4 page components pass tests (MonitoringPage 4 tests, AlertPage 5 tests, HistorianPage 5 tests, DispatchPage 5 tests = 19 total)
    - Stale badges, priority colors, no-candidate messaging, denied access, lifecycle buttons, rule labels all render correctly
    - Form submissions call correct APIs and handle responses
    - No test failures or missing assertions
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebSocket realtime hook tests</name>
  <files>frontend/src/hooks/useAlertRealtime.test.ts</files>
  
  <behavior>
    - **Connect**: Hook establishes WebSocket on mount; receives invalidation messages; triggers refetch
    - **Disconnect**: WebSocket closes (network error, server shutdown); hook detects loss; status changes to "offline"
    - **Reconnect**: After disconnect, hook automatically attempts reconnect with exponential backoff (1s, 2s, 4s, 8s, max 16s)
    - **Message Handling**: Receives invalidation event {type: "invalidate", resource: "alert"} → triggers refetch of alert state via REST
    - **Cleanup**: On unmount, WebSocket closed and reconnect interval cleared
  </behavior>

  <action>
Create `frontend/src/hooks/useAlertRealtime.test.ts` using Vitest and React hooks testing library (renderHook, act from @testing-library/react):

**Test 1: WebSocket connects on mount**
- Mock window.WebSocket with MockWebSocket
- renderHook(() => useAlertRealtime(patient_id))
- Assert WebSocket constructor called with correct URL (wss://... or ws://... with patient_id)
- Assert hook state is "connected"

**Test 2: WebSocket receives invalidation and triggers refetch**
- Mock WebSocket with message handler
- Mock fetch for REST refetch
- renderHook(() => useAlertRealtime(patient_id))
- act(() => { mockWebSocket.send({type: "invalidate", resource: "alert"}) })
- Assert fetch called to GET alert
- Assert hook refetch count incremented

**Test 3: WebSocket disconnect detected**
- Mock WebSocket with close handler
- renderHook(() => useAlertRealtime(patient_id))
- act(() => { mockWebSocket.close() })
- Assert hook state changes to "offline" or "disconnected"
- Assert "Offline" UI state available

**Test 4: Automatic reconnect with exponential backoff**
- Mock WebSocket with close handler
- Use fake timers (vi.useFakeTimers())
- renderHook(() => useAlertRealtime(patient_id))
- act(() => { mockWebSocket.close() })
- Assert state is "offline"
- vi.advanceTimersByTime(1000) → assert reconnect attempt at 1s
- Mock reconnect failure
- vi.advanceTimersByTime(2000) → assert reconnect attempt at 3s total (1+2)
- Continue: 4s, 8s, 16s (cap)
- Eventually mock successful reconnect
- Assert state returns to "connected"

**Test 5: Multiple disconnects and reconnects**
- Similar to Test 4, but disconnect/reconnect twice
- Verify backoff counter resets after successful reconnect
- Second disconnect reconnects with fresh 1s backoff (not continuing from 16s)

**Test 6: Cleanup on unmount**
- renderHook(() => useAlertRealtime(patient_id))
- Mock WebSocket
- Unmount hook
- Assert WebSocket.close() called
- Assert reconnect interval cleared (setInterval not running)
- Assert no memory leaks

**Test 7: Manual page reload / force-refresh button**
- renderHook(() => useAlertRealtime(patient_id))
- Mock WebSocket and fetch
- Simulate user clicking "Refresh" button (which calls hook's refetch method)
- Assert fetch called immediately (not waiting for invalidation)
- Assert alert data refetched via REST
  </action>

  <verify>
    <automated>npm --prefix frontend run test -- --run useAlertRealtime</automated>
  </verify>

  <done>
    - WebSocket connect/disconnect/reconnect behavior tested with 7 test cases
    - Exponential backoff verified (1s, 2s, 4s, 8s, 16s)
    - Invalidation messages trigger REST refetch
    - Cleanup on unmount verified
    - No test failures or memory leaks
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend component → Mocked fetch API | Components assume stable API contract; mocks validate contract assumptions |
| WebSocket hook → Browser WebSocket API | Hook wraps native WebSocket; mocks simulate connection/disconnection events |
| User actions → Form submission | Components must validate and sanitize user input before sending to API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Information Disclosure | Denied access UI | medium | mitigate | Assert error message is generic (no patient-specific language) |
| T-05-14 | Denial of Service | WebSocket reconnect loop | medium | mitigate | Exponential backoff caps at 16s; does not retry infinitely |
| T-05-15 | Tampering | Form input validation | low | mitigate | Tests verify form submission calls API; API-side validation tested separately |

</threat_model>

<verification>
Run frontend test suites; verify output shows:
- All tests pass with 0 failures (19 page component tests + 7 WebSocket tests = 26 total)
- No console errors or warnings
- Test coverage for all critical UI paths
- Stale/denied/fallback/no-candidate states handled correctly in UI
</verification>

<success_criteria>
Frontend components render correctly, handle all operational states (stale, denied, no-candidate, fallback), manage WebSocket lifecycle (connect/disconnect/reconnect), and submit forms with correct API calls.
</success_criteria>

<output>
Create `.planning/phases/05-end-to-end-verification-and-demo-hardening/05-03-SUMMARY.md` when complete.
</output>
