---
phase: 05-end-to-end-verification-and-demo-hardening
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - backend/tests/test_phase5_clean_reset.py
  - backend/tests/test_phase5_degraded_states.py
  - backend/tests/test_phase5_full_journey.py
  - backend/tests/conftest.py
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 35000
  raw_tokens: 18000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Reset/reseed is idempotent: running twice on the same database produces identical state (exactly three accounts, no alerts/assignments/audit, fresh fixture data)."
    - "All Phase 1-4 critical paths remain intact and testable with fresh reset."
    - "Degraded states (stale dispatch, fallback prediction, denied access, no-candidate alert) preserve data integrity without spurious mutations."
    - "Full backend journey can be replayed deterministically with injected clock and produces ordered audit evidence."
  artifacts:
    - path: "backend/tests/test_phase5_clean_reset.py"
      provides: "Reset idempotence, cleanup ordering, and state verification"
    - path: "backend/tests/test_phase5_degraded_states.py"
      provides: "Stale/fallback/denied/no-candidate scenarios with audit trail validation"
    - path: "backend/tests/test_phase5_full_journey.py"
      provides: "End-to-end API journey with deterministic clock and audit reconstruction"
  key_links:
    - from: "backend/tests/test_phase5_clean_reset.py"
      to: "backend/app/seed/reset.py"
      via: "Reset ordering must delete Phase 4-1 children before parents"
    - from: "backend/tests/test_phase5_degraded_states.py"
      to: "backend/app/contracts/dispatch.py and backend/app/contracts/auth.py"
      via: "Stale dispatch, fallback, and denied checks verify API contract enforcement"
    - from: "backend/tests/test_phase5_full_journey.py"
      to: "backend/app/audit/repository.py"
      via: "Audit events must be persisted in insertion order with stable IDs"
---

<objective>
Expand backend coverage beyond the tracer: verify reset robustness, test all degraded states and edge cases, and prove full API journey with deterministic replay.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-end-to-end-verification-and-demo-hardening/05-RESEARCH.md
@backend/tests/test_phase4_integration.py
@backend/tests/conftest.py
@backend/app/seed/reset.py
@backend/app/seed/demo_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset idempotence and cleanup ordering</name>
  <files>backend/tests/test_phase5_clean_reset.py, backend/tests/conftest.py</files>
  
  <action>
Create `backend/tests/test_phase5_clean_reset.py` with three test cases:

**Test 1: Reset is Foreign-Key Safe and Deletes in Child-First Order**
- Setup: Create temp SQLite, run migrations, seed P-1042 fixture
- Manually create alerts, assignments, dispatch evaluations, audit events (Phase 3-4 descendants)
- Call `reset_demo_data(session)` 
- Verify deletion order:
  * Annotations/decisions (Phase 4 children) deleted first
  * Dispatch evaluations and rule evaluations (Phase 3-4) deleted
  * Alert/context/prediction (Phase 2-3 parents) cleaned
  * Vital observations (Phase 1 children) removed
  * Verify no foreign-key constraint violations
- Assert no dangling references remain

**Test 2: Reset Idempotence — Running Twice Produces Identical State**
- Setup: Create temp SQLite, seed P-1042
- Create P-1042 alert by advancing vitals
- Run reset_demo_data() once
- Snapshot database state: count users, nurses, patients, beds, context facts, configurations
- Run reset_demo_data() again
- Verify second snapshot matches first snapshot exactly (row counts identical)
- Assert second reset is a no-op (no exceptions, no extra deletions)

**Test 3: Reseed Restores Neutral State**
- Setup: Create temp SQLite, migrations, reset
- Call seed_demo_data(session)
- Verify:
  * Exactly 3 users: Admin, Doctor, Nurse (Sarah)
  * Exactly 1 patient: P-1042
  * Exactly 1 admission, 1 ICU bed assignment
  * Exactly 4 context facts (baseline risk, 3 medical context facts)
  * Exactly 1 version of rules (rules.v1)
  * Sarah candidate inputs populated (location, availability, workload baseline)
  * Configuration: prototype thresholds, refresh settings, ML provider reference
  * NO alerts, assignments, dispatch evaluations, or audit events
  * Clock set to fixed datetime (2026-01-01 UTC)
  </action>

  <verify>
    <automated>python -m pytest backend/tests/test_phase5_clean_reset.py -v</automated>
  </verify>

  <done>
    - Reset deletes all Phase 4-1 artifacts in foreign-key-safe order
    - Reset is idempotent (running twice on same database produces same state)
    - Reseed restores exactly the expected fixture: 3 users, 1 P-1042, 4 context facts, no alerts
    - All assertions pass with no foreign-key violations
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Degraded state scenarios (stale, fallback, denied, no-candidate)</name>
  <files>backend/tests/test_phase5_degraded_states.py</files>
  
  <behavior>
    - **Stale Dispatch**: Dispatch evaluation created at T; user attempts confirm at T+20min → 409 "evaluation stale"; refresh succeeds; confirm with new evaluation succeeds
    - **Fallback Prediction**: ML provider unavailable/error → prediction uses deterministic fallback with source_kind="deterministic_fallback" and fallback_reason populated
    - **Denied Access**: Unassigned nurse tries to read P-1042 alert → 403 with generic reason (not leaking patient name); audit records denied outcome
    - **No-Candidate Alert**: All eligible nurses filtered out → evaluation returns no candidates; alert stays "generated" (not "assigned"); reason "No eligible nurses available" visible
  </behavior>

  <action>
Create `backend/tests/test_phase5_degraded_states.py` with four test cases:

**Test 1: Stale Dispatch Evaluation Blocks Confirmation**
- Setup: Client with make_client(), fixed clock at T=10:00
- Create dispatch evaluation for P-1042 alert
- Store evaluation_id and creation_timestamp
- Advance clock to T=10:20 (20 minutes later)
- Attempt POST `/api/v1/patients/P-1042/dispatch/confirm` with original evaluation_id
- Assert 409 Conflict with message containing "stale" or "expired"
- POST `/api/v1/patients/P-1042/dispatch/evaluation` to refresh
- New evaluation_id returned
- POST `/api/v1/patients/P-1042/dispatch/confirm` with new evaluation_id
- Assert 200, assignment created with fresh evaluation reference
- Assert audit event shows refresh and confirmation with timestamps

**Test 2: Fallback Prediction When ML Provider Unavailable**
- Setup: Create app with null/disabled ML provider config
- Advance P-1042 to threshold (ticks 0-3)
- POST `/api/v1/patients/P-1042/dispatch/evaluation` as Doctor
- Assert prediction.source_kind == "deterministic_fallback"
- Assert prediction.fallback_reason contains "provider unavailable" or similar
- Assert prediction.risk_score is numeric and deterministic
- Assert prediction does NOT include model_version, model_name, or model_metadata
- Assert dispatch candidates ranked without model_score component
- Verify audit event logs "fallback_prediction_used"

**Test 3: Denied Access Logged Without Identity Leak**
- Setup: Create seeded Alex (unassigned nurse)
- Alex logs in and receives valid Bearer token
- Alex attempts GET `/api/v1/patients/P-1042/alert` with Bearer token
- Assert 403 Forbidden
- Assert response body does not contain patient name, bed, diagnosis, or alert content
- Assert response is generic: `{"detail": "Access denied"}` or similar (no role/patient-specific message)
- Query audit trail: assert one record with actor="U-ALEX", action="read", resource="alert", outcome="denied"
- Assert audit detail does NOT leak patient-identifying info (reason is generic: "not_assigned" not "Alex is not assigned to P-1042")

**Test 4: No-Candidate State Preserves Alert as Generated**
- Setup: Create all candidate nurses and mark each as unavailable:
  * Sarah: on_break=True
  * James (if exists): in_surgery=True
  * Others: offline status or acuity mismatch
- Advance P-1042 vitals to critical threshold
- GET `/api/v1/patients/P-1042/alert` → assert state="generated"
- POST `/api/v1/patients/P-1042/dispatch/evaluation` as Doctor
- Assert response.outcome == "no_candidate_available" or similar
- Assert response.candidates is empty array or null
- Assert response.exclusions array lists Sarah, James, others with reason
- GET `/api/v1/patients/P-1042/alert` → assert state still "generated", assignment_id null
- Assert audit includes "dispatch_evaluation_no_candidate" event
- Verify no unassigned nurses received alert notification
  </action>

  <verify>
    <automated>python -m pytest backend/tests/test_phase5_degraded_states.py -v</automated>
  </verify>

  <done>
    - Stale dispatch evaluation blocks confirmation until refreshed
    - Fallback prediction correctly labeled when ML unavailable; no model metadata leaked
    - Denied access returns 403 without identity/PHI leakage; audit records outcome
    - No-candidate alert stays generated and unassigned; reason visible; no nurses notified
  </done>
</task>

<task type="auto">
  <name>Task 3: Full backend journey with deterministic replay and audit trail</name>
  <files>backend/tests/test_phase5_full_journey.py</files>
  
  <action>
Create `backend/tests/test_phase5_full_journey.py` with one comprehensive journey test:

**Test: Complete P-1042 Journey from Setup to Resolution with Ordered Audit Trail**
- Setup: make_client() with fixed clock, seeded data, admin/doctor/sarah logins
- Step 1: Admin advances vitals ticks 0→1→2→3 (deterministic progression)
  * Assert each tick returns 200 + new vital snapshot
  * Verify vital values change predictably (HR increases, SpO2 decreases per scenario)
- Step 2: Config update (Admin sets critical threshold to 0.25)
  * Assert 200, configuration persisted
- Step 3: At tick 3 or 4, check if alert created
  * If not automatically created, advance one more tick
  * Assert GET alert returns 200 + alert with state="generated"
- Step 4: Doctor fetches historian
  * Assert GET historian returns 200 + complete context (demographics, diagnoses, meds, labs, timeline, baseline/contextual risk)
- Step 5: Doctor evaluates dispatch
  * Assert POST evaluation returns 200 + candidates sorted by score
  * Verify Sarah is top candidate (highest score)
- Step 6: Admin confirms dispatch with Sarah
  * Assert POST confirm returns 200 + assignment created
  * Assert alert state transitions to "assigned"
  * Assert audit_id created for assignment
- Step 7: Sarah logs in and acknowledges
  * Assert POST acknowledge returns 200
  * Assert alert state transitions to "acknowledged"
- Step 8: Sarah responds with note
  * Assert POST respond returns 200
  * Assert response_note persisted
- Step 9: Sarah resolves
  * Assert POST resolve returns 200
  * Assert alert state transitions to "resolved"
- Step 10: Verify audit trail
  * Assert GET audit returns 200 + ≥15 events (configuration, advances, alert, dispatch, confirm, acknowledge, respond, resolve, any denials)
  * Assert events are ordered by timestamp (ascending or with stable tiebreaker IDs)
  * Assert each event has required fields: event_id, actor_id, action, resource_type, outcome, timestamp, detail (if applicable)
  * Sample assertions on specific events:
    - Configuration update: detail includes old/new threshold values
    - Alert creation: detail includes risk_score, threshold_value
    - Dispatch confirmation: detail includes confirmed_nurse_id, score, confidence
    - Lifecycle events: detail includes state_before, state_after
  * Assert no event contains password, bearer token, or PHI beyond patient_id
- Step 11: Verify audit reconstruction matches live state
  * Replay lifecycle events from audit; verify final state matches current alert state
  * Reconstruct nurse assignment chain; verify assignment_id matches current assignment
  </action>

  <verify>
    <automated>python -m pytest backend/tests/test_phase5_full_journey.py::test_complete_p1042_journey_with_audit_trail -v</automated>
  </verify>

  <done>
    - P-1042 journey completes successfully from deterioration through resolution
    - Audit trail contains ≥15 ordered events with all required fields and no credential leakage
    - Each lifecycle transition (generated → assigned → acknowledged → responded → resolved) is recorded
    - Dispatch confirmation reason and score recorded in audit detail
    - Audit trail can be replayed to reconstruct complete workflow state
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reset code → Database schema | Reset must respect foreign-key relationships; no orphaned rows |
| Audit repository → Persisted events | All events immutable after creation; ordered by timestamp |
| API endpoints → Role-based responses | Denied requests return 403 without PHI leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering | Reset ordering | high | mitigate | Test verifies child deletion before parents; assert no FK violations |
| T-05-09 | Repudiation | Audit trail | high | mitigate | Assert each event is immutable and ordered; cannot be backdated or reordered |
| T-05-10 | Information Disclosure | Denied access audit | high | mitigate | Audit reason field uses generic codes (e.g., "not_assigned") not patient-specific messages |
| T-05-11 | Denial of Service | No-candidate infinite retry | medium | mitigate | No automatic assignment created when candidates empty; Admin retry only |
| T-05-12 | Tampering | Stale evaluation bypass | medium | mitigate | Confirm requires fresh evaluation; old evaluation_id rejected with 409 |

</threat_model>

<verification>
Run all three backend test suites; verify output shows:
- All tests pass with 0 failures
- Reset is idempotent (no difference between first and second run)
- Stale/fallback/denied/no-candidate scenarios handled correctly
- Audit trail contains ≥15 ordered events
- No credentials appear in test output or assertions
</verification>

<success_criteria>
Backend infrastructure is robust: reset/reseed work correctly, all degraded states are handled safely without data corruption, and full API journey can be replayed deterministically with complete ordered audit evidence.
</success_criteria>

<output>
Create `.planning/phases/05-end-to-end-verification-and-demo-hardening/05-02-SUMMARY.md` when complete.
</output>
