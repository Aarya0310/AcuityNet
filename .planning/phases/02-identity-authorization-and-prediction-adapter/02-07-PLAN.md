---
phase: 02-identity-authorization-and-prediction-adapter
plan: 07
date: 2026-08-24
type: execute
wave: 5
depends_on: [02-04]
files_modified: [backend/app/admin/repository.py, backend/app/admin/configuration.py, backend/tests/test_admin_repository.py]
autonomous: true
requirements: [PRED-04, ADMIN-01]
estimate: {tokens: 9000, raw_tokens: 9000, tasks: 2, confidence: low}
must_haves:
  truths: ["Admin user, Nurse, Bed, and typed Configuration changes persist transactionally through SQLAlchemy.", "Relationships and stable seeded account boundaries survive repository operations."]
  artifacts: ["backend/app/admin/repository.py", "backend/app/admin/configuration.py", "backend/tests/test_admin_repository.py"]
  key_links: ["Admin service boundary in 02-04 -> repository -> ORM session", "typed configuration persistence -> effective settings consumed by PredictionAdapter"]
---
<objective>Implement the coherent Admin persistence and typed-configuration storage expansion supporting primary Admin-01/PRED-04 plan 02-04.</objective>
<execution_context>@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-04-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-PATTERNS.md
@backend/app/persistence/models.py</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Wire repository-backed Admin persistence</name><files>backend/app/admin/repository.py, backend/app/admin/configuration.py, backend/tests/test_admin_repository.py</files><behavior><ul><li>get_user, create_user, update_user, update_nurse_status, and update_bed persist User, Nurse, and Bed changes.</li><li>User.nurse/Nurse.user and Bed.patient_id relationships remain intact.</li><li>Typed Configuration read/write operations commit atomically and preserve exactly three demo accounts; the unassigned Nurse remains test-only.</li></ul></behavior><action>Per ADMIN-01 and PRED-04, implement session-bound repository operations and an explicit configuration repository dependency using the existing SQLAlchemy transaction patterns. Validate uniqueness, role, assignment, and allowlisted configuration values before commit; rollback the complete mutation on failure. Keep repository code limited to persistence and do not add later alert, dispatch, or audit entities.</action><verify><automated>python -m pytest backend/tests/test_admin_repository.py -q</automated></verify><done>The Admin service boundary from 02-04 has durable repository-backed User/Nurse/Bed/Configuration operations with relationship and rollback coverage.</done></task>
<task type="auto" tdd="true"><name>Task 2: Expand persistence and reseed regression coverage</name><files>backend/app/admin/repository.py, backend/app/admin/configuration.py, backend/tests/test_admin_repository.py</files><behavior><ul><li>Repeated repository reads/writes return typed effective settings and stable identifiers.</li><li>Invalid or duplicate mutations leave all persisted rows unchanged.</li></ul></behavior><action>Use real temporary SQLite migrations and seed fixtures to test stable IDs, foreign-key-safe relationships, exact demo account counts, and no partial writes after invalid Admin mutations. Preserve the deterministic test-only unassigned Nurse fixture without adding it to demo seed counts.</action><verify><automated>python -m pytest backend/tests/test_admin_repository.py backend/tests/test_phase2_seed.py -q</automated></verify><done>Persistence remains deterministic, relationship-safe, and compatible with reset/reseed and the prediction adapter.</done></task>
</tasks>
<threat_model><table><tr><th>ID</th><th>Category</th><th>Severity</th><th>Disposition</th><th>Mitigation</th></tr><tr><td>T-02-16</td><td>Tampering</td><td>high</td><td>mitigate</td><td>Typed allowlists, transaction rollback, and real FK relationship tests.</td></tr><tr><td>T-02-17</td><td>Elevation of privilege</td><td>high</td><td>mitigate</td><td>Repository exposes only service-bound operations; route authorization remains in 02-04.</td></tr></table></threat_model>
<verification><automated>python -m pytest backend/tests/test_admin_repository.py backend/tests/test_phase2_seed.py -q</automated></verification>
<success_criteria>02-04 Admin routes and configuration wiring have the durable repository/configuration implementation and focused persistence proof they require.</success_criteria>
<output>Create .planning/phases/02-identity-authorization-and-prediction-adapter/02-07-SUMMARY.md when done.</output>
