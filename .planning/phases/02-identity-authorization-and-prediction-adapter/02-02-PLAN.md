---
phase: 02-identity-authorization-and-prediction-adapter
plan: 02
date: 2026-08-24
type: execute
wave: 3
depends_on: [02-01, 02-04]
files_modified: [backend/app/contracts/predictions.py, backend/app/contracts/metadata.py, backend/app/prediction/__init__.py, backend/app/prediction/fallback.py, backend/app/prediction/adapter.py, backend/app/transport/predictions.py, backend/app/main.py, backend/tests/test_predictions.py, frontend/src/contracts/predictions.ts, frontend/src/api/client.ts, frontend/src/prediction/PredictionPage.tsx, frontend/src/prediction/PredictionPage.test.tsx, frontend/src/App.tsx]
autonomous: true
requirements: [PRED-01, PRED-02, PRED-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An authorized role can request one stable prediction payload for the latest P-1042 observation."
    - "Identical observation/configuration inputs produce identical fallback output with explicit event, probability, score, level, horizon, rule version, and fallback reason."
    - "An injected valid ML provider is used and labeled as ML; unavailable, failing, or invalid providers select and label the deterministic fallback without source laundering."
    - "The Prognosticator renders prediction fields, current vitals, provenance, source/version metadata, and the non-clinical prototype label without deriving risk in React."
  artifacts:
    - "backend/app/contracts/predictions.py"
    - "backend/app/prediction/adapter.py"
    - "backend/app/prediction/fallback.py"
    - "frontend/src/prediction/PredictionPage.tsx"
    - "backend/tests/test_predictions.py"
  key_links:
    - "authorized prediction GET -> latest persisted synthetic observation -> adapter -> versioned response DTO"
    - "ML capability branch/fallback branch -> common contract with source and safety metadata"
    - "prediction response -> typed API client -> Clinical Prognosticator"
---
<objective>
Deliver a stable prediction contract and one authorized Clinical Prognosticator path over the authoritative latest synthetic observation.

Purpose: Later alert, historian, and dispatch phases need a predictable server result, while this phase must make model availability and fallback behavior visible rather than implicit.
Output: Versioned backend/frontend prediction DTOs, injected ML seam, deterministic fallback, protected prediction route, and focused branch/UI tests.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-RESEARCH.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-PATTERNS.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-01-PLAN.md
@backend/app/contracts/vitals.py
@backend/app/safety/labels.py
@backend/app/main.py
@frontend/src/monitoring/MonitoringPage.tsx
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Wire one protected P-1042 prediction from observation to Prognosticator</name>
  <files>backend/app/contracts/predictions.py, backend/app/contracts/metadata.py, backend/app/prediction/__init__.py, backend/app/prediction/fallback.py, backend/app/prediction/adapter.py, backend/app/transport/predictions.py, backend/app/main.py, backend/tests/test_predictions.py, frontend/src/contracts/predictions.ts, frontend/src/api/client.ts, frontend/src/prediction/PredictionPage.tsx, frontend/src/prediction/PredictionPage.test.tsx, frontend/src/App.tsx</files>
  <behavior>
    - "An authenticated permitted caller receives patient ID, bed ID, prediction event, probability, risk score, risk level, horizon, timestamp, current vitals, synthetic provenance, prototype label, prediction contract version, source kind, and model/rule metadata."
    - "A missing latest observation returns a resource error and never fabricates a prediction."
    - "The fallback branch is selected when no provider is injected and its output is repeatable for the same observation/configuration."
    - "The UI displays risk/event/probability/horizon/current vitals/source/version and safety labeling from the server response."
  </behavior>
  <action>Define a Pydantic prediction contract with `extra=forbid`, closed risk-level/source/fallback states, explicit contract version, source name, model version or rule version, fallback reason/status, timestamp, and the existing `SyntheticProvenance` plus `PROTOTYPE_LABEL`. Define a typed adapter protocol accepting the latest `VitalObservation` and effective prediction settings. Implement a pure versioned fallback rule using named vital thresholds/deltas from the current observation, with deterministic bounded score, probability, event, level, and horizon values; do not use randomness, wall-clock input, or clinical claims. Implement provider selection so an injected provider is used only when available and contract-valid, and all other cases clearly report deterministic fallback. Add an authenticated `GET /api/v1/patients/{patient_id}/prediction` route that applies policy before querying the latest row and keeps route coordination thin. Mirror the exact serialized shape in TypeScript, add one API client method, and compose the page into the authenticated app while retaining server safety metadata. Do not add a model package or make React calculate risk. This is the PRED-01/PRED-02/PRED-03 tracer path.</action>
  <verify><automated>python -m pytest backend/tests/test_predictions.py -q; npm --prefix frontend run test -- --run src/prediction/PredictionPage.test.tsx; npm --prefix frontend run build</automated></verify>
  <done>One authenticated role can load the complete P-1042 prediction result and see whether it came from the injected ML seam or deterministic fallback; repeated fallback requests are stable and fully labeled.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expand prediction branch, contract, and degraded-state coverage</name>
  <files>backend/app/prediction/adapter.py, backend/app/prediction/fallback.py, backend/app/contracts/predictions.py, backend/tests/test_predictions.py, frontend/src/prediction/PredictionPage.tsx, frontend/src/prediction/PredictionPage.test.tsx</files>
  <behavior>
    - "A valid injected ML result preserves its model name/version and source kind; provider absence, provider exception, and provider contract failure all use the fallback source and explain why."
    - "Probability, score, risk level, horizon, and version fields stay within contract bounds, and invalid provider output cannot cross the API boundary."
    - "The UI presents loading, unavailable, fallback, stale-vitals, and API-denied states without implying bedside truth or validated clinical output."
  </behavior>
  <action>Strengthen the adapter and contract tests around every source-selection branch and malformed provider result. Keep one deterministic fallback rule version and explicit non-clinical metadata in every successful prediction response. Add page tests for semantic field rendering and operational degraded states; use the centralized bearer-aware client and server freshness/provenance rather than local risk arithmetic. Leave alert creation and lifecycle transitions to Phase 3.</action>
  <verify><automated>python -m pytest backend/tests/test_predictions.py backend/tests/test_authorization.py -q; npm --prefix frontend run test -- --run src/prediction/PredictionPage.test.tsx; npm --prefix frontend run lint</automated></verify>
  <done>Prediction source behavior, contract bounds, unavailable/fallback labeling, and UI degraded states are deterministic and regression-tested.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Protected API -> prediction adapter | Persisted synthetic vitals and Admin settings become risk-like output. |
| Optional provider -> server contract | Provider output is untrusted even when injected by the application. |
| Prediction API -> browser UI | Risk-like values and source metadata cross into a clinical-looking surface. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Tampering | provider output | high | mitigate | Validate provider output against closed Pydantic contract before response. |
| T-02-06 | Spoofing | prediction source metadata | high | mitigate | Server selects and emits source/model/rule metadata; React cannot infer source. |
| T-02-07 | Information disclosure | prediction route | high | mitigate | Reuse Phase 2 identity and patient policy before latest-observation lookup. |
| T-02-08 | Repudiation | prediction timestamp/version | medium | mitigate | Include observation timestamp, prediction timestamp, contract version, and rule/model version. |
</threat_model>

<verification>
<automated>python -m pytest backend/tests/test_predictions.py -q; npm --prefix frontend run test -- --run src/prediction/PredictionPage.test.tsx; npm --prefix frontend run build; npm --prefix frontend run lint</automated>
</verification>

<success_criteria>The protected prediction endpoint returns one stable, versioned, synthetic-safe shape across ML and fallback branches, and the Prognosticator visibly identifies all required fields and source state.</success_criteria>

<output>Create `.planning/phases/02-identity-authorization-and-prediction-adapter/02-02-SUMMARY.md` when done.</output>
