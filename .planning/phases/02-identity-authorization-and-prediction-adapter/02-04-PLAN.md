---
phase: 02-identity-authorization-and-prediction-adapter
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified: [frontend/src/navigation/AppShell.tsx, frontend/src/dashboards/AdminDashboardView.tsx, frontend/src/dashboards/DoctorDashboardView.tsx, frontend/src/dashboards/NurseDashboardView.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/styles.css, frontend/src/**/*.test.tsx, backend/tests/test_phase2_integration.py, scripts/phase2_smoke.py, README.md]
autonomous: true
requirements: [UI-01]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin, Doctor, and Nurse see distinct permitted navigation and dashboard projections after authentication."
    - "Doctor has read-oriented monitoring/prediction access; Nurse sees assignment-scoped monitoring work; Admin sees controls/KPIs, with no UI path presented as a security boundary."
    - "A seeded Windows-friendly smoke path proves login, role projection, prediction source, Admin configuration/KPI access, and logout against a clean SQLite app."
    - "Every role-facing risk-like surface retains simulated ICU, research-prototype, provenance, and non-clinical labeling."
  artifacts:
    - "frontend/src/navigation/AppShell.tsx"
    - "frontend/src/dashboards/AdminDashboardView.tsx"
    - "frontend/src/dashboards/DoctorDashboardView.tsx"
    - "frontend/src/dashboards/NurseDashboardView.tsx"
    - "scripts/phase2_smoke.py"
  key_links:
    - "current-user role -> AppShell navigation/projection"
    - "role projection -> protected REST resources -> existing safety components"
    - "clean migration/seed -> smoke login -> prediction/configuration/KPI -> logout"
---
<objective>
Finish the role-aware browser experience and executable Phase 2 integration evidence across the identity, prediction, and Admin slices.

Purpose: The server policy is the authority, but users need visibly different workflows to understand what each role can do and to exercise the full Phase 2 contract in a repeatable local run.
Output: Role dashboards/navigation, integration regressions, a Windows-friendly Phase 2 smoke runner, and setup documentation limited to Phase 2 behavior.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-RESEARCH.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-PATTERNS.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-01-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-02-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-03-PLAN.md
@frontend/src/App.tsx
@frontend/src/monitoring/MonitoringPage.tsx
@frontend/src/safety/PrototypeBanner.tsx
@frontend/src/safety/ProvenanceBadge.tsx
@scripts/phase1_smoke.py
@README.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Wire role-specific shell and dashboards over completed REST contracts</name>
  <files>frontend/src/navigation/AppShell.tsx, frontend/src/dashboards/AdminDashboardView.tsx, frontend/src/dashboards/DoctorDashboardView.tsx, frontend/src/dashboards/NurseDashboardView.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/styles.css, frontend/src/**/*.test.tsx</files>
  <action>Compose the authenticated shell from the current-user role and render exactly three role projections: Admin navigation to configuration/KPIs and operational controls; Doctor navigation to read-oriented monitoring and prediction; Nurse navigation to assignment-scoped monitoring/work. Use the existing React Query provider, centralized API client, typed contracts, and existing monitoring/prediction/Admin components rather than duplicating fetch or policy logic. Preserve safety banners, server provenance, explicit loading/error/unauthorized/unavailable/fallback states, and responsive accessible layout. Ensure role changes/session expiry remove protected views and logout clears all protected query state. Add component tests that assert visible role-appropriate navigation and absence of Admin controls for Doctor/Nurse, while documenting that UI hiding is only presentation and backend tests enforce authorization.</action>
  <verify><automated>npm --prefix frontend run test -- --run; npm --prefix frontend run build; npm --prefix frontend run lint</automated></verify>
  <done>Each seeded role lands in a distinct usable dashboard projection with the required prediction/monitoring/admin content and safety labeling; frontend tests prove the visible role boundary.</done>
</task>
<task type="auto">
  <name>Task 2: Add Phase 2 integration smoke and documentation checks</name>
  <files>backend/tests/test_phase2_integration.py, scripts/phase2_smoke.py, README.md</files>
  <action>Extend backend integration coverage and add a standard-library Windows-friendly smoke runner that starts the existing Uvicorn app against an isolated SQLite database, waits for health, logs in as all three seeded accounts, verifies current-user and protected-route status differences, reads the stable prediction/fallback metadata, verifies Admin-only configuration/KPI access, logs out, and tears down in `finally`. Update README setup/reset instructions with deterministic development credentials handling, `ACUITYNET_JWT_SECRET`, migration/seed commands, and explicit research-prototype scope. Keep the smoke limited to Phase 2; do not claim alert, historian, dispatch, lifecycle, audit, WebSocket, or clinical behavior that belongs to later phases.</action>
  <verify><automated>python -m pytest backend/tests/test_phase2_integration.py backend/tests/test_auth.py backend/tests/test_authorization.py backend/tests/test_predictions.py backend/tests/test_admin.py -q; python scripts/phase2_smoke.py; npm --prefix frontend run test -- --run; npm --prefix frontend run build</automated></verify>
  <done>A clean local run proves the Phase 2 identity, policy, prediction-source, Admin-control, KPI, and logout journey, and README instructions reproduce it without exposing secrets or overstating capability.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser role shell -> REST API | Client navigation and route parameters are untrusted presentation inputs. |
| Smoke runner -> local app | Seeded credentials and bearer tokens exercise the same protected boundary as a browser. |
| README/developer setup -> local secret store | Instructions must not turn demo credentials or JWT secrets into committed production secrets. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-13 | Elevation of privilege | role dashboard navigation | high | mitigate | Pair visible projections with backend direct-call authorization tests and smoke assertions. |
| T-02-14 | Information disclosure | frontend query/cache state | medium | mitigate | Clear protected query state on logout/401 and render explicit denied/unavailable branches. |
| T-02-15 | Repudiation | Phase 2 smoke evidence | low | accept | Phase 3 owns durable audit events; this smoke records deterministic assertions only. |
| T-02-16 | Information disclosure | setup documentation | medium | mitigate | Document development-only credential handling and environment secret use without committing secret values. |
</threat_model>

## Source Audit
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Role-limited behavior plus stable explicit-source prediction | 02-01, 02-02, 02-03, 02-04 | COVERED | Identity, policy, prediction, Admin, and role UI form the complete path. |
| REQ | AUTH-01..AUTH-04 | Seeded JWT login/logout and server rejection | 02-01 | COVERED | Migration, seed, bearer dependency, logout, and denial matrix. |
| REQ | UI-01 | Three role dashboards/navigation | 02-01, 02-04 | COVERED | Auth shell tracer plus final role projections. |
| REQ | PRED-01..PRED-03 | Stable prediction shape, ML/fallback adapter, Prognosticator | 02-02 | COVERED | DTO, adapter branches, route, UI, and tests. |
| REQ | PRED-04 | Admin-editable thresholds/research configuration | 02-03 | COVERED | Typed allowlisted settings consumed by prediction. |
| REQ | ADMIN-01..ADMIN-02 | Admin management controls and operational KPIs | 02-03 | COVERED | Typed Admin routes/read model and dashboard. |
| RESEARCH | security | Fixed JWT algorithm, password KDF, current DB role, policy checks | 02-01 | COVERED | Security checkpoint and threat register. |
| RESEARCH | prediction | Injected ML seam and deterministic versioned fallback, no ML install | 02-02 | COVERED | Both source branches and failure behavior tested. |
| RESEARCH | config | Typed settings at both edges, no arbitrary key/value writes | 02-03 | COVERED | Transactional allowlist and effective settings. |
| RESEARCH | UI/authority | React Query, centralized bearer client, REST authoritative | 02-01, 02-04 | COVERED | Session/query shell and smoke/recovery assertions. |
| RESEARCH | safety | Synthetic provenance and prototype label on prediction/config surfaces | 02-02, 02-03, 02-04 | COVERED | Existing safety contracts/components are reused. |
| CONTEXT | none | No Phase 2 CONTEXT.md exists | - | COVERED | No locked Phase 2 decisions or deferred ideas beyond project artifacts. |

<verification>
<automated>python -m pytest backend/tests/test_phase2_integration.py backend/tests/test_auth.py backend/tests/test_authorization.py backend/tests/test_predictions.py backend/tests/test_admin.py -q; python scripts/phase2_smoke.py; npm --prefix frontend run test -- --run; npm --prefix frontend run build; npm --prefix frontend run lint</automated>
</verification>

<success_criteria>The complete Phase 2 journey is executable from clean SQLite setup: each role authenticates into its permitted projection, direct API bypasses fail correctly, prediction source and metadata are explicit, Admin controls/KPIs are honest and typed, and all safety boundaries remain visible.</success_criteria>

<output>Create `.planning/phases/02-identity-authorization-and-prediction-adapter/02-04-SUMMARY.md` when done.</output>
