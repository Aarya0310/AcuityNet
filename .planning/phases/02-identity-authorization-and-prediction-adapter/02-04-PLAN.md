---
phase: 02-identity-authorization-and-prediction-adapter
plan: 04
date: 2026-08-24
type: execute
wave: 2
depends_on: [02-01]
files_modified: [backend/app/auth/policy.py, backend/app/main.py, backend/tests/test_authorization.py, backend/tests/test_vitals_api.py, frontend/src/api/client.ts, frontend/src/contracts/auth.ts, frontend/src/auth/AuthContext.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/auth/AuthContext.test.tsx]
autonomous: true
requirements: [AUTH-03, UI-01]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Named protected routes enforce role, patient resource, and nurse assignment policy server-side, independent of hidden UI navigation."
    - "Admin, Doctor, assigned Nurse Sarah, and unassigned Nurse Alex produce the explicit 401/403 matrix for current-vitals and advance calls."
    - "The browser logs in, restores /api/v1/auth/me, clears token/query state on logout or 401, and renders a bounded role projection."
  artifacts:
    - "backend/app/auth/policy.py"
    - "backend/tests/test_authorization.py"
    - "frontend/src/auth/AuthContext.tsx"
    - "frontend/src/auth/ProtectedRoute.tsx"
  key_links:
    - "get_current_user -> require_roles/require_patient_access/require_nurse_assignment -> protected routes"
    - "unassigned Nurse fixture -> policy denial before patient lookup or mutation"
    - "centralized bearer client -> AuthContext -> ProtectedRoute/App shell"
---
<objective>
Make the identity usable without making the frontend a security boundary.

Purpose: AUTH-03 requires direct API enforcement for role, resource, and assignment ownership, while UI-01 needs a session-aware shell.
Output: Explicit policy symbols, protected Phase 1 routes, authorization matrix fixtures/tests, and frontend session/guard wiring.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-RESEARCH.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-PATTERNS.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-01-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-02-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-03-PLAN.md
@frontend/src/App.tsx
@frontend/src/monitoring/MonitoringPage.tsx
@frontend/src/safety/PrototypeBanner.tsx
@frontend/src/safety/ProvenanceBadge.tsx
@scripts/phase1_smoke.py
@README.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Wire explicit authorization policy through protected monitoring routes</name>
  <files>backend/app/auth/policy.py, backend/app/main.py, backend/tests/test_authorization.py, backend/tests/test_vitals_api.py</files>
  <action>Define exact exported symbols get_current_user, require_roles, require_patient_access, and require_nurse_assignment in backend/app/auth/policy.py and apply them to GET /api/v1/patients/{patient_id}/vitals/current and POST /api/v1/patients/{patient_id}/vitals/advance in backend/app/main.py. Use assigned Nurse Sarah and unassigned Nurse Alex fixtures. Implement test_authorization.py rows: anonymous -> 401; Admin P-1042 read/advance -> 200; Doctor P-1042 read -> 200 and advance -> 403; assigned Sarah read -> 200 and advance -> 403; unassigned Alex read/advance -> 403; any authenticated user P-999 -> 403. Assert denied calls do not mutate observations and direct HTTP calls prove UI bypass is irrelevant. Update test_vitals_api.py fixtures for the protected route. Preserve REST-authoritative advance-then-read behavior and exact 401/403 semantics for AUTH-03.</action>
  <verify><automated>python -m pytest backend/tests/test_authorization.py backend/tests/test_vitals_api.py -q</automated></verify>
  <done>The named protected routes enforce the complete role/resource/assignment matrix through server policy symbols and direct API tests.</done>
</task>
<task type="auto">
  <name>Task 2: Add frontend session restore, protected guard, and role projection</name>
  <files>frontend/src/api/client.ts, frontend/src/contracts/auth.ts, frontend/src/auth/AuthContext.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/auth/AuthContext.test.tsx</files>
  <action>Extend frontend/src/api/client.ts as the sole bearer/error boundary with typed login/me/logout calls and 401 session-expiry handling. Add AuthContext, LoginPage, and ProtectedRoute using the existing TanStack Query provider and explicit loading/error branches; store only the documented short-lived local prototype session representation. Compose App.tsx so /api/v1/auth/me restoration gates protected views and logout/401 clears token and protected query state. Add semantic tests for login, restore, logout, 401 clearing, protected redirect, and role projection. UI hiding is presentation only; Task 1's direct API matrix remains the authorization authority.</action>
  <verify><automated>npm --prefix frontend run test -- --run src/auth/AuthContext.test.tsx; npm --prefix frontend run build; npm --prefix frontend run lint</automated></verify>
  <done>Browser session lifecycle is tested and protected UI cannot render without a current session, while direct API authorization is covered by Task 1.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser role shell -> REST API | Client navigation and route parameters are untrusted presentation inputs. |
| Smoke runner -> local app | Seeded credentials and bearer tokens exercise the same protected boundary as a browser. |
| README/developer setup -> local secret store | Instructions must not turn demo credentials or JWT secrets into committed production secrets. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-13 | Elevation of privilege | role dashboard navigation | high | mitigate | Pair visible projections with backend direct-call authorization tests and smoke assertions. |
| T-02-14 | Information disclosure | frontend query/cache state | medium | mitigate | Clear protected query state on logout/401 and render explicit denied/unavailable branches. |
| T-02-15 | Repudiation | Phase 2 smoke evidence | low | accept | Phase 3 owns durable audit events; this smoke records deterministic assertions only. |
| T-02-16 | Information disclosure | setup documentation | medium | mitigate | Document development-only credential handling and environment secret use without committing secret values. |
</threat_model>

## Source Audit
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Role-limited behavior plus stable explicit-source prediction | 02-01, 02-02, 02-03, 02-04 | COVERED | Identity, policy, prediction, Admin, and role UI form the complete path. |
| REQ | AUTH-01..AUTH-04 | Seeded JWT login/logout and server rejection | 02-01 | COVERED | Migration, seed, bearer dependency, logout, and denial matrix. |
| REQ | UI-01 | Three role dashboards/navigation | 02-01, 02-04 | COVERED | Auth shell tracer plus final role projections. |
| REQ | PRED-01..PRED-03 | Stable prediction shape, ML/fallback adapter, Prognosticator | 02-02 | COVERED | DTO, adapter branches, route, UI, and tests. |
| REQ | PRED-04 | Admin-editable thresholds/research configuration | 02-03 | COVERED | Typed allowlisted settings consumed by prediction. |
| REQ | ADMIN-01..ADMIN-02 | Admin management controls and operational KPIs | 02-03 | COVERED | Typed Admin routes/read model and dashboard. |
| RESEARCH | security | Fixed JWT algorithm, password KDF, current DB role, policy checks | 02-01 | COVERED | Security checkpoint and threat register. |
| RESEARCH | prediction | Injected ML seam and deterministic versioned fallback, no ML install | 02-02 | COVERED | Both source branches and failure behavior tested. |
| RESEARCH | config | Typed settings at both edges, no arbitrary key/value writes | 02-03 | COVERED | Transactional allowlist and effective settings. |
| RESEARCH | UI/authority | React Query, centralized bearer client, REST authoritative | 02-01, 02-04 | COVERED | Session/query shell and smoke/recovery assertions. |
| RESEARCH | safety | Synthetic provenance and prototype label on prediction/config surfaces | 02-02, 02-03, 02-04 | COVERED | Existing safety contracts/components are reused. |
| CONTEXT | none | No Phase 2 CONTEXT.md exists | - | COVERED | No locked Phase 2 decisions or deferred ideas beyond project artifacts. |

<verification>
<automated>python -m pytest backend/tests/test_phase2_integration.py backend/tests/test_auth.py backend/tests/test_authorization.py backend/tests/test_predictions.py backend/tests/test_admin.py -q; python scripts/phase2_smoke.py; npm --prefix frontend run test -- --run; npm --prefix frontend run build; npm --prefix frontend run lint</automated>
</verification>

<success_criteria>The complete Phase 2 journey is executable from clean SQLite setup: each role authenticates into its permitted projection, direct API bypasses fail correctly, prediction source and metadata are explicit, Admin controls/KPIs are honest and typed, and all safety boundaries remain visible.</success_criteria>

<output>Create `.planning/phases/02-identity-authorization-and-prediction-adapter/02-04-SUMMARY.md` when done.</output>
