---
phase: 02-identity-authorization-and-prediction-adapter
plan: 05
date: 2026-08-24
type: execute
wave: 5
depends_on: [02-02, 02-03, 02-04]
files_modified: [frontend/src/navigation/AppShell.tsx, frontend/src/dashboards/AdminDashboardView.tsx, frontend/src/dashboards/DoctorDashboardView.tsx, frontend/src/dashboards/NurseDashboardView.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/styles.css, frontend/src/**/*.test.tsx, backend/tests/test_phase2_integration.py, scripts/phase2_smoke.py, README.md]
autonomous: true
requirements: [UI-01]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Admin, Doctor, and Nurse see distinct permitted navigation and dashboard projections after authentication, while the API remains the authority."
    - "A seeded Windows-friendly smoke run sets ACUITYNET_JWT_SECRET in the child process environment, uses documented deterministic accounts, and proves login, role projection, protected denial, prediction source, Admin controls/KPIs, logout, reset, and reseed."
    - "Every role-facing risk-like surface retains simulated ICU, research-prototype, provenance, fallback, and non-clinical labeling."
  artifacts: [frontend/src/navigation/AppShell.tsx, frontend/src/dashboards/AdminDashboardView.tsx, frontend/src/dashboards/DoctorDashboardView.tsx, frontend/src/dashboards/NurseDashboardView.tsx, scripts/phase2_smoke.py, backend/tests/test_phase2_integration.py]
  key_links: ["current-user role -> AppShell projection", "role projection -> protected REST resources", "clean reset -> migration/seed -> smoke login -> prediction/admin -> logout"]
---
<objective>
Finish role projections and executable Phase 2 integration evidence without expanding into later workflow phases.
Purpose: The end-to-end check must prove the plan's identity, policy, prediction, Admin, and safety boundaries on Windows-friendly local setup.
Output: Role dashboards, integration regression, smoke runner with explicit secret/account setup, and README instructions.
</objective>
<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-01-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-02-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-03-PLAN.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-04-PLAN.md
@scripts/phase1_smoke.py
@README.md
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Wire the three role dashboards over completed protected contracts</name>
  <files>frontend/src/navigation/AppShell.tsx, frontend/src/dashboards/AdminDashboardView.tsx, frontend/src/dashboards/DoctorDashboardView.tsx, frontend/src/dashboards/NurseDashboardView.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/styles.css, frontend/src/**/*.test.tsx</files>
  <action>Compose one authenticated AppShell from the current-user role with exactly three projections: Admin sees management/KPIs, Doctor sees read-oriented monitoring/prediction, and Nurse sees assignment-scoped work. Reuse the centralized client, TanStack Query, prediction/Admin/monitoring components, safety banner, provenance, and server operational states. Keep ProtectedRoute and session expiry behavior explicit, clear protected queries on logout, and add semantic component tests for each role's visible links plus absence of Admin mutations for Doctor/Nurse. Do not claim UI hiding is authorization; direct API bypasses are proven in backend tests. Preserve exact research-prototype and simulated-ICU labeling.</action>
  <verify><automated>npm --prefix frontend run test -- --run; npm --prefix frontend run build; npm --prefix frontend run lint</automated></verify>
  <done>Each exact role lands in a distinct tested dashboard projection with safety metadata and no unrestricted hospital view.</done>
</task>
<task type="auto">
  <name>Task 2: Add integration smoke, reset/reseed evidence, and setup documentation</name>
  <files>backend/tests/test_phase2_integration.py, scripts/phase2_smoke.py, README.md</files>
  <action>Extend backend integration coverage and add a standard-library Windows-friendly smoke runner. Before starting Uvicorn, require or generate an explicit process-local development secret and pass it only through the child `ACUITYNET_JWT_SECRET` environment; fail fast if the secret is absent rather than embedding a committed value. Use deterministic seeded development account credentials from the README/local fixture, never print password or secret values, and assert health. The runner must call migration/seed against an isolated SQLite database, verify reset deletes dependent observations/assignments safely, reseed restores stable P-1042/ICU-12/N-SARAH rows and expected user/assignment/configuration counts, log in all three accounts, verify current-user, anonymous/invalid/wrong-role/wrong-patient/unassigned-Nurse statuses, read prediction source/metadata, verify Admin management/KPI access and a configuration change affects the next prediction, log out, and tear down in finally. Update README with exact environment setup (`$env:ACUITYNET_JWT_SECRET = ...`), deterministic development account names/password handling, migration/seed/reset commands, and prototype scope. Keep Phase 2 claims limited to identity, authorization, prediction, Admin controls/KPIs, and reset/reseed; do not claim alerts, historian, dispatch, lifecycle, audit, WebSockets, or clinical behavior.</action>
  <verify><automated>python -m pytest backend/tests/test_phase2_integration.py backend/tests/test_phase2_migration.py backend/tests/test_phase2_seed.py backend/tests/test_auth.py backend/tests/test_authorization.py backend/tests/test_predictions.py backend/tests/test_admin.py -q; python scripts/phase2_smoke.py; npm --prefix frontend run test -- --run; npm --prefix frontend run build</automated></verify>
  <done>Clean Windows-friendly setup proves the complete Phase 2 path, including explicit secret/account setup, direct authorization denials, Admin-to-adapter wiring, logout, and reset/reseed stability.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser role shell -> REST API | Navigation and route parameters are untrusted presentation inputs. |
| Smoke runner -> local app | Seeded credentials and bearer tokens exercise the protected boundary. |
| README/environment -> local secret | Setup must not turn development credentials or JWT secrets into committed production secrets. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-14 | Elevation of privilege | role projections/API | high | mitigate | Pair visible projections with direct API matrix and smoke assertions. |
| T-02-15 | Information disclosure | client/query state | medium | mitigate | Clear protected state on logout/401 and test protected redirect. |
| T-02-16 | Information disclosure | smoke/setup | medium | mitigate | Environment-only secret, no secret/password output, and explicit fail-fast setup. |
| T-02-17 | Tampering | reset/reseed path | high | mitigate | Isolated database smoke plus dependent-row deletion and stable-count assertions. |
</threat_model>
<source_audit>
| Source | ID | Feature/Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | - | Role-limited behavior plus stable explicit-source prediction | 02-01 through 02-05 | COVERED |
| REQ | AUTH-01..AUTH-04 | Seeded JWT identity and server rejection | 02-01, 02-04 | COVERED |
| REQ | UI-01 | Three role dashboards/navigation | 02-02, 02-05 | COVERED |
| REQ | PRED-01..PRED-04 | Stable adapter, fallback, Prognosticator, Admin settings | 02-03, 02-04 | COVERED |
| REQ | ADMIN-01..ADMIN-02 | Management areas and KPIs | 02-04, 02-05 | COVERED |
| RESEARCH | security/config/prediction/UI/safety | Fixed JWT, typed config, injected fallback, REST authority, labels | 02-01 through 02-05 | COVERED |
| CONTEXT | none | No Phase 2 CONTEXT.md exists | - | COVERED |
</source_audit>
<verification><automated>python -m pytest backend/tests/test_phase2_integration.py backend/tests/test_phase2_migration.py backend/tests/test_phase2_seed.py backend/tests/test_auth.py backend/tests/test_authorization.py backend/tests/test_predictions.py backend/tests/test_admin.py -q; python scripts/phase2_smoke.py; npm --prefix frontend run test -- --run; npm --prefix frontend run build; npm --prefix frontend run lint</automated></verification>
<success_criteria>Phase 2 is reproducible from clean SQLite setup: exact roles authenticate, direct API bypasses fail, prediction source/configuration is explicit, Admin controls/KPIs are honest, safety labels persist, and reset/reseed are verified.</success_criteria>
<output>Create .planning/phases/02-identity-authorization-and-prediction-adapter/02-05-SUMMARY.md when done.</output>
