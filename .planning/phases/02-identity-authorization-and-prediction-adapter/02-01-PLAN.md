---
phase: 02-identity-authorization-and-prediction-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/pyproject.toml, backend/app/persistence/models.py, backend/app/migrations/versions/0002_identity_authorization.py, backend/app/seed/demo_data.py, backend/app/seed/reset.py, backend/app/contracts/auth.py, backend/app/auth/security.py, backend/app/auth/service.py, backend/app/auth/dependencies.py, backend/app/auth/policy.py, backend/app/transport/auth.py, backend/app/main.py, backend/tests/test_phase2_migration.py, backend/tests/test_phase2_seed.py, backend/tests/test_auth.py, backend/tests/test_authorization.py, frontend/src/api/client.ts, frontend/src/contracts/auth.ts, frontend/src/auth/AuthContext.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/auth/AuthContext.test.tsx]
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, UI-01]
user_setup:
  - service: PyJWT
    why: "JWT signing and verification for the local seeded prototype"
    env_vars:
      - name: ACUITYNET_JWT_SECRET
        source: "Generate a local development-only secret; never commit it"
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Exactly seeded Admin, Doctor, and Nurse identities can establish a short-lived bearer session and retrieve their current identity."
    - "Missing, malformed, expired, disabled, unknown-subject, and unknown-role sessions cannot reach protected routes; authenticated but disallowed calls receive a distinct forbidden response."
    - "Current-vitals reads and bounded fixture advancement are protected by server-side role/resource policy, including patient and nurse-assignment scope."
    - "The browser can log in, restore a session, log out, and render a bounded role projection without treating hidden navigation as authorization."
  artifacts:
    - "backend/app/migrations/versions/0002_identity_authorization.py"
    - "backend/app/auth/dependencies.py"
    - "backend/app/auth/policy.py"
    - "backend/tests/test_auth.py"
    - "frontend/src/auth/AuthContext.tsx"
  key_links:
    - "Alembic revision -> User/role/assignment rows -> idempotent seed"
    - "Bearer token -> current-user database lookup -> closed-role/resource policy -> protected route"
    - "login response -> centralized API bearer header -> current-user query -> ProtectedRoute/App shell"
---
<objective>
Deliver the first usable identity slice: a seeded user can log in, reach an authorized P-1042 monitoring view, and log out while the API enforces the same policy independently of the UI.

Purpose: Phase 1 deliberately left mutations public and has no identity boundary. Establishing one durable, server-checked session path gives all later prediction and Admin behavior a trustworthy owner.
Output: Migration-backed users and assignment scope, JWT auth service/dependencies, protected Phase 1 routes, seeded accounts, and a minimal browser session shell.
</objective>

<execution_context>
@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-RESEARCH.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-PATTERNS.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-01-SUMMARY.md
@.planning/phases/01-safety-simulation-and-backend-contracts/01-06-SUMMARY.md
@backend/app/main.py
@backend/app/persistence/models.py
@backend/app/seed/demo_data.py
@frontend/src/api/client.ts
@frontend/src/App.tsx
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the selected JWT package before installation</name>
  <action>Verify the official PyPI project identity, release metadata, source repository, and current package legitimacy for PyJWT 2.13.0 using `pypi.org/project/PyJWT/` and the repository linked there. Confirm the local development secret will be supplied through `ACUITYNET_JWT_SECRET` and never committed. Do not approve an alternate JWT package or an ML dependency.</action>
  <verify><automated>MISSING — blocking human verification must confirm PyJWT legitimacy and local secret setup before Task 2</automated></verify>
  <done>Human verification explicitly approves the researched PyJWT package and confirms a development-only JWT secret source.</done>
  <what-built>Package legitimacy and local-secret gate for the JWT dependency.</what-built>
  <how-to-verify>Inspect the official PyPI project and linked source repository, then confirm the executor can provide `ACUITYNET_JWT_SECRET` without committing it.</how-to-verify>
  <resume-signal>Reply with approval to install the verified PyJWT package and continue with the identity tracer.</resume-signal>
</task>
<task type="tracer" tdd="true">
  <name>Task 2: Wire seeded login through protected P-1042 monitoring</name>
  <files>backend/pyproject.toml, backend/app/persistence/models.py, backend/app/migrations/versions/0002_identity_authorization.py, backend/app/seed/demo_data.py, backend/app/seed/reset.py, backend/app/contracts/auth.py, backend/app/auth/security.py, backend/app/auth/service.py, backend/app/auth/dependencies.py, backend/app/auth/policy.py, backend/app/transport/auth.py, backend/app/main.py, backend/tests/test_phase2_migration.py, backend/tests/test_phase2_seed.py, backend/tests/test_auth.py, backend/tests/test_authorization.py, frontend/src/api/client.ts, frontend/src/contracts/auth.ts, frontend/src/auth/AuthContext.tsx, frontend/src/auth/LoginPage.tsx, frontend/src/auth/ProtectedRoute.tsx, frontend/src/App.tsx, frontend/src/auth/AuthContext.test.tsx</files>
  <behavior>
    - "POST login with each seeded account returns a bearer token and current-user payload without password material."
    - "A valid token is revalidated against the current active database user and closed role; changing or disabling that user invalidates the protected request even if the JWT claim is unchanged."
    - "GET current vitals is available only to authenticated users within declared resource scope; POST bounded advance is limited to the explicitly chosen development/admin capability and rejects anonymous, wrong-role, wrong-patient, and out-of-assignment calls."
    - "The browser login form stores the selected short-lived session representation, sends it through one API wrapper, restores `/auth/me`, clears it on logout or 401, and renders a role-aware protected shell."
  </behavior>
  <action>After the package checkpoint, add a migration after `0001_phase1_foundation` for a closed `Role` representation, active `User` rows, password digest fields, and the minimum patient/nurse assignment relationship needed to prove resource scope. Preserve P-1042, ICU-12, N-SARAH, SQLite foreign keys, migration-first setup, and reset ordering. Seed exactly one fictional Admin, Doctor, and Nurse with deterministic local credentials documented only for development; hash passwords with salted `hashlib.scrypt` and constant-time comparison, never plaintext. Implement `security.py` with PyJWT, a fixed algorithm allowlist, configured secret, subject/issued-at/expiry claims, and generic credential failures; implement `dependencies.py` to load the current active user from the database on every protected request. Implement `policy.py` with explicit role guards and patient/assignment checks, returning 401 for missing/invalid identity and 403 for authenticated denial. Add login, current-user, and logout semantics, protect the existing current-vitals and advance routes in `main.py`, and keep REST/session state authoritative. In the frontend, extend `client.ts` as the sole bearer/error boundary, add typed auth contracts/provider/login/protected guard, and preserve the existing safety banner and monitoring advance-then-current-GET behavior. Treat the browser token as a local research-prototype session representation, not production identity infrastructure. Per AUTH-01 through AUTH-04 and UI-01, do not accept role values from request bodies or rely on navigation visibility for authorization.</action>
  <verify><automated>python -m pytest backend/tests/test_phase2_migration.py backend/tests/test_phase2_seed.py backend/tests/test_auth.py backend/tests/test_authorization.py -q; npm --prefix frontend run test -- --run src/auth/AuthContext.test.tsx; npm --prefix frontend run build</automated></verify>
  <done>Fresh SQLite migration and seed produce exactly three accepted roles and three usable demo accounts; login/me/logout and protected P-1042 routes pass the authorization matrix; the frontend can authenticate and render only the current role projection.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser -> login/session API | Credentials, bearer tokens, role-shaped client state, and patient IDs are untrusted. |
| FastAPI -> SQLite identity store | Password digests, active status, roles, and assignments control access. |
| API -> protected monitoring mutation | A valid identity crosses into persistent synthetic observation state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Spoofing | JWT login/dependency | high | mitigate | PyJWT verification with fixed algorithm, expiry, configured secret, and database subject lookup. |
| T-02-02 | Elevation of privilege | role/patient/assignment policy | high | mitigate | Closed role enum plus server-side policy dependencies and direct API denial tests. |
| T-02-03 | Information disclosure | protected patient routes | high | mitigate | Authenticate before resource lookup and enforce patient/assignment scope. |
| T-02-04 | Information disclosure | password/session contracts | high | mitigate | Salted scrypt digests, generic login failure, no password fields, and client clearing on 401/logout. |
| T-02-SC | Tampering | PyJWT install | high | mitigate | Blocking legitimacy checkpoint against the official PyPI project before installation. |
</threat_model>

<verification>
<automated>python -m pytest backend/tests/test_phase2_migration.py backend/tests/test_phase2_seed.py backend/tests/test_auth.py backend/tests/test_authorization.py -q; npm --prefix frontend run test -- --run src/auth/AuthContext.test.tsx; npm --prefix frontend run build</automated>
</verification>

<success_criteria>Seeded users can authenticate and log out; all protected Phase 1 behavior has explicit server authorization; exact roles and assignment scope are tested; the browser has a working login/protected-shell path with safety metadata retained.</success_criteria>

<output>Create `.planning/phases/02-identity-authorization-and-prediction-adapter/02-01-SUMMARY.md` when done.</output>
