---
phase: 02-identity-authorization-and-prediction-adapter
plan: 03
date: 2026-08-24
type: execute
wave: 3
depends_on: [02-02]
files_modified: [backend/app/contracts/predictions.py, backend/app/contracts/metadata.py, backend/app/prediction/__init__.py, backend/app/prediction/adapter.py, backend/app/prediction/fallback.py, backend/app/transport/predictions.py, backend/app/main.py, backend/tests/test_predictions.py, frontend/src/contracts/predictions.ts, frontend/src/api/client.ts, frontend/src/prediction/PredictionPage.tsx, frontend/src/prediction/PredictionPage.test.tsx, frontend/src/App.tsx]
autonomous: true
requirements: [PRED-01, PRED-02, PRED-03]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths: ["An authorized user receives a stable prediction from the latest observation.", "ML and deterministic fallback sources are explicit and validated.", "The Prognosticator renders server prediction and safety metadata without local risk arithmetic."]
  artifacts: ["backend/app/prediction/adapter.py", "backend/app/prediction/fallback.py", "backend/tests/test_predictions.py", "frontend/src/prediction/PredictionPage.tsx"]
  key_links: ["protected prediction GET -> latest observation -> PredictionAdapter.predict -> DTO", "DTO -> bearer client -> Prognosticator"]
---
<objective>Deliver the stable prediction tracer over authoritative synthetic vitals.</objective>
<execution_context>@.github/gsd-core/workflows/execute-plan.md
@.github/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-RESEARCH.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-PATTERNS.md
@.planning/phases/02-identity-authorization-and-prediction-adapter/02-02-PLAN.md
@backend/app/contracts/vitals.py
@backend/app/vitals/service.py
@backend/app/vitals/scenario.py
@backend/app/safety/labels.py
@frontend/src/monitoring/MonitoringPage.tsx</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Wire latest observation through PredictionAdapter to Prognosticator</name><files>backend/app/contracts/predictions.py, backend/app/contracts/metadata.py, backend/app/prediction/__init__.py, backend/app/prediction/adapter.py, backend/app/prediction/fallback.py, backend/app/transport/predictions.py, backend/app/main.py, backend/tests/test_predictions.py, frontend/src/contracts/predictions.ts, frontend/src/api/client.ts, frontend/src/prediction/PredictionPage.tsx, frontend/src/prediction/PredictionPage.test.tsx, frontend/src/App.tsx</files><action>Follow 02-PATTERNS.md contract analogs contracts/vitals.py, contracts/configuration.py, and safety/labels.py. Define extra-forbid DTOs with patient, bed, event, probability, score, level, horizon, timestamp, current vitals, provenance, prototype label, contract version, source kind, model/rule version, and fallback reason/status. Follow vitals/scenario.py and vitals/service.py for pure deterministic handling. Add PredictionAdapter.predict(latest_observation, effective_settings) and a versioned fallback with configurable research thresholds, no randomness or wall clock. Expose GET /api/v1/patients/{patient_id}/prediction, authorize before lookup, and mirror the shape in TypeScript.</action><verify><automated>python -m pytest backend/tests/test_predictions.py -q; npm --prefix frontend run test -- --run src/prediction/PredictionPage.test.tsx; npm --prefix frontend run build</automated></verify><done>One authenticated in-scope role sees complete prediction and explicit ML/fallback metadata.</done></task>
<task type="auto" tdd="true"><name>Task 2: Expand provider, fallback, and degraded-state tests</name><files>backend/app/prediction/adapter.py, backend/app/prediction/fallback.py, backend/app/contracts/predictions.py, backend/tests/test_predictions.py, frontend/src/prediction/PredictionPage.tsx, frontend/src/prediction/PredictionPage.test.tsx</files><behavior><ul><li>Provider absence, exception, and invalid output select labeled deterministic fallback.</li><li>Identical observation/settings inputs are identical and bounded.</li><li>Missing observation, unavailable, stale, denied, and fallback states remain distinct.</li></ul></behavior><action>Use 02-PATTERNS.md adapter analogs and MonitoringPage.tsx state/cleanup analog. Validate every provider result before response; preserve synthetic provenance and exact prototype label; keep React from deriving score or level. Leave Admin settings consumption to 02-04.</action><verify><automated>python -m pytest backend/tests/test_predictions.py -q; npm --prefix frontend run test -- --run src/prediction/PredictionPage.test.tsx; npm --prefix frontend run lint</automated></verify><done>All source branches and operational states are deterministic and tested.</done></task>
</tasks>
<threat_model><table><tr><th>ID</th><th>Category</th><th>Severity</th><th>Disposition</th><th>Mitigation</th></tr><tr><td>T-02-05</td><td>Tampering</td><td>high</td><td>mitigate</td><td>Validate provider results against closed DTO.</td></tr><tr><td>T-02-06</td><td>Spoofing</td><td>high</td><td>mitigate</td><td>Server emits source/version and safety metadata.</td></tr></table></threat_model>
<verification><automated>python -m pytest backend/tests/test_predictions.py -q; npm --prefix frontend run test -- --run src/prediction/PredictionPage.test.tsx; npm --prefix frontend run build; npm --prefix frontend run lint</automated></verification>
<success_criteria>The prediction endpoint and Prognosticator share one stable explicit-source contract.</success_criteria>
<output>Create .planning/phases/02-identity-authorization-and-prediction-adapter/02-03-SUMMARY.md when done.</output>
